---
phase: 05-insights-blog-system
plan: 05
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - src/content/blog/<seed-slug>.mdx
  - src/content/blog/placeholder-post.mdx
  - .planning/CLIENT_DISCLOSURE_CLEARANCE.md
autonomous: false
requirements:
  - BLOG-09
  - LEGAL-09
tags:
  - content
  - human-gate
  - publish

must_haves:
  truths:
    - "Exactly one non-draft blog MDX file exists in src/content/blog/ once this plan completes"
    - "The published post is authored by Jon Van Loo (`author: jon-van-loo`) and tagged `practiceArea: tax` — D-03 / D-04"
    - "The body text of the published post is Jon's draft verbatim — Claude has formatted (headings, lists, links, code blocks where Jon used them) but NOT rewritten, paraphrased, or added examples (D-01 / D-02 content-fidelity rule)"
    - "`npm run lint:legal` passes on the new post — no `expert` / `specialist` / `specialize` hits except where the existing allowlist permits, and Jon has reviewed any banned-term hits"
    - "Any client / counterparty / deal-value reference in the post body is present in `.planning/CLIENT_DISCLOSURE_CLEARANCE.md` (added in this plan if needed and only after Jon's explicit clearance)"
    - "The Phase 1 `src/content/blog/placeholder-post.mdx` is deleted in the same commit that publishes the real seed post (Pitfall 9 — never an intermediate state with zero posts)"
    - "After build, `/blog` shows the seed post in the list; `/blog/<seed-slug>` renders with the full chrome from 05-02; `/blog/rss.xml` contains one item with the seed post's content (T-05-03 / T-05-04 invariants preserved)"
    - "Jon has reviewed the rendered Vercel preview URL and signed off (the resume-signal from the human-action checkpoint)"
  artifacts:
    - path: "src/content/blog/<seed-slug>.mdx"
      provides: "First published BSV Insights post — Jon-authored crypto/blockchain tax piece"
      contains: "draft: false"
    - path: ".planning/CLIENT_DISCLOSURE_CLEARANCE.md"
      provides: "Updated clearance register if the seed post references any client/counterparty/deal-value"
      contains: "<post-slug>"
  key_links:
    - from: "src/content/blog/<seed-slug>.mdx frontmatter"
      to: "src/content/attorneys/jon-van-loo.mdx"
      via: "author: jon-van-loo reference"
      pattern: "author:\\s*jon-van-loo"
    - from: "src/content/blog/<seed-slug>.mdx frontmatter"
      to: "src/content/practiceAreas/tax.mdx"
      via: "practiceArea: tax reference"
      pattern: "practiceArea:\\s*tax"
---

<objective>
**MVP Slice 4 — publish the seed post.** This plan is **NOT autonomous**. It runs after 05-02, 05-03, and 05-04 have shipped the pipeline. Per D-05 / D-14, it has a hard-stop human-action checkpoint where Jon supplies his draft body text, Claude formats it into the MDX file, surfaces any `lint:legal` hits to Jon, waits for Jon's confirmation, then flips `draft: false`, deletes the placeholder, runs the build, and reports the Vercel preview URL.

This is the vertical-slice payoff for BLOG-09 and the substantive (human-gated) closure of LEGAL-09. The phase cannot close on `draft: true` alone — BLOG-09 requires one published post live (D-05).

Purpose: Mirror the FAQ-DRAFT.md / D-13 gate from Phase 4 (Plan 04-04). The pipeline is fully automated up through 05-04; the publish-act itself is human-gated because the content is Jon's attorney-of-record work product, subject to Rule 7.1 / 7.4 substantive review that no automation can perform.

Output: One non-draft `.mdx` file in `src/content/blog/` (with verbatim Jon-authored body), the deletion of `placeholder-post.mdx`, optional CLIENT_DISCLOSURE_CLEARANCE.md update, and a green production-preview URL Jon has reviewed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-insights-blog-system/05-CONTEXT.md
@.planning/phases/05-insights-blog-system/05-RESEARCH.md
@.planning/phases/04-attorney-practice-area-pages/04-CONTEXT.md
@.planning/phases/04-attorney-practice-area-pages/04-04-PLAN.md
@.planning/CLIENT_DISCLOSURE_CLEARANCE.md
@CLAUDE.md
@.claude/CLAUDE.md
@.planning/phases/05-insights-blog-system/05-01-SUMMARY.md
@.planning/phases/05-insights-blog-system/05-02-SUMMARY.md
@.planning/phases/05-insights-blog-system/05-03-SUMMARY.md
@.planning/phases/05-insights-blog-system/05-04-SUMMARY.md

<interfaces>
<!-- Patterns Claude must mirror in the format step. -->

src/content/blog/placeholder-post.mdx (current — to be deleted on publish commit):
- author: "placeholder-attorney" (references a placeholder; safe because draft:true filters it from the loader)
- draft: true

src/content/attorneys/jon-van-loo.mdx — verify Jon's attorney entry exists (it does, Phase 4 04-02). The seed post's `author: jon-van-loo` references this entry by slug.

src/content/practiceAreas/tax.mdx — verify Tax practice exists (it does, Phase 4 04-03). The seed post's `practiceArea: tax` references this entry by slug.

src/content.config.ts blog schema (lines 64-78 + 05-01 Wave-0 .refine()):
- Required fields: title, slug, author (reference), practiceArea (reference), publishedAt (coerce.date), summary, draft (default false)
- Optional fields: updatedAt, cover, coverAlt — refined so if cover is set, coverAlt is non-empty

scripts/lint-legal.mjs — Phase 4 prebuild scanner over `src/content/**/*.mdx`. The allowlist for "expert" / "specialist" is empty by default (Phase 4 04-01). Stuart's USPTO registration phrasing is the only allowlisted occurrence (Phase 4 D-04).

URL-CONVENTIONS slug rule (Phase 1 D-11..D-14): blog slugs are kebab-case, lowercased, never renamed post-launch.

PERF-02 image budget: 200KB per committed image. If Jon supplies a cover image, this is enforced by the existing Phase 1 assets-budget.spec.ts.

Phase 4 04-04 (the FAQ-DRAFT D-13 gate) is the exact precedent — same human-action-checkpoint shape: present options to Jon, wait for response, format into MDX, run lint:legal, surface hits, wait for confirmation, commit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the seed-post MDX file with draft:true (pre-checkpoint preparation)</name>
  <files>src/content/blog/<seed-slug-placeholder>.mdx</files>
  <read_first>
    - src/content/blog/placeholder-post.mdx (verify it still has draft:true — do NOT touch it in this task)
    - src/content.config.ts blog schema (confirm required field names)
    - .planning/phases/05-insights-blog-system/05-CONTEXT.md § Seed post (D-03 / D-04 / D-05) lines 326-340 — frontmatter scaffold
    - .planning/phases/05-insights-blog-system/05-RESEARCH.md § Seed post scaffold (lines 1058-1075) — locked MDX scaffold
    - src/content/attorneys/jon-van-loo.mdx (verify the slug field is `jon-van-loo` — defensive check)
    - src/content/practiceAreas/tax.mdx (verify the slug field is `tax` — defensive check)
  </read_first>
  <action>
    Step A — Surface options to Jon (CLI message, plain English):
    Before creating the file, display this message verbatim and STOP:

      "Phase 5 is ready for your seed Insights post. The pipeline is built — when you paste a draft below, I'll:
       1. Pick a slug for the file based on the title you give me (kebab-case, never renamed once published per URL-CONVENTIONS).
       2. Scaffold the MDX file at `src/content/blog/<slug>.mdx` with `draft: true` so it stays invisible while we format it.
       3. Run `npm run lint:legal` on the body and surface any banned-term hits (Rule 7.4: 'expert', 'specialist', 'specialize').
       4. Surface any client / counterparty / deal-value reference for you to confirm against `.planning/CLIENT_DISCLOSURE_CLEARANCE.md` before publish.
       5. Wait for your sign-off.

       Before I scaffold the file, three small choices:

       (a) What is the post title? (We'll derive the slug from it — e.g., title 'IRS Notice 2025-X and DeFi yield' → slug `irs-notice-2025-x-and-defi-yield`.)

       (b) 1-2 sentence summary for the `/blog` list + RSS description?

       (c) Cover image strategy — pick one:
           • SVG illustration (under 200KB; we can commission a custom abstract crypto-tax glyph or reuse the existing hero motif vocabulary)
           • Photograph (under 200KB WebP) — note: most BSV Insights posts will be aniconic (no photo); skipping cover renders cleanly per UI-SPEC
           • Omit cover for v1 (recommended — the post title + byline is enough; we can add a cover later)

       Paste the title, summary, and cover choice as a single response, then in a follow-up message paste the body text. I will not write or paraphrase the body — that's yours by content-fidelity rule (D-01)."

    DO NOT create the MDX file yet — wait for Jon's response with title, summary, and cover choice.

    Step B — After Jon supplies the title + summary + cover choice (resume from his message):
    1. Derive the slug: lowercase the title, replace non-alphanumeric with `-`, collapse repeated `-`, trim leading/trailing `-`. Surface the derived slug to Jon as a single line for confirmation: "Proposed slug: `<derived-slug>`. URL will be `https://bsvlaw.com/blog/<derived-slug>` — confirm or supply a different slug." If Jon confirms or supplies an override, proceed.
    2. Create `src/content/blog/<slug>.mdx` with this exact frontmatter (replacing placeholders):
       ```
       ---
       title: "<Jon's title — escaped for YAML>"
       slug: "<slug>"
       author: jon-van-loo
       practiceArea: tax
       publishedAt: <today's date in YYYY-MM-DD>
       summary: "<Jon's summary — escaped for YAML>"
       draft: true
       <only if Jon chose SVG/photo cover, add:>
       cover: ./images/<slug>-cover.<ext>
       coverAlt: "<Jon supplies — or we ask>"
       ---

       <!-- Body lands here when Jon pastes the draft text. Until then, leave this comment as a placeholder. -->
       ```
       The publishedAt date is today's ISO date (`new Date().toISOString().slice(0,10)`). Jon may want to backdate or forward-date — surface the date to him for confirmation in the same response.
    3. Run `npm run check` to verify the Zod schema accepts the frontmatter (the references to `jon-van-loo` and `tax` MUST resolve; if either is missing or draft:true, the build fails with a typed Zod error — surface the error to Jon plainly).
    4. Do NOT yet run `lint:legal` (the body is empty).
    5. Hand off to Task 2 for the human-action checkpoint that gathers Jon's body text.

    DO NOT write the body text. DO NOT paraphrase Jon's title or summary. DO NOT add `updatedAt` (only emit if Jon edits an already-published post — out of scope here). DO NOT add `reviewedBy` (D-13 forbids; field is gone from schema).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dir='src/content/blog';const files=fs.readdirSync(dir).filter(f=>f.endsWith('.mdx')&&f!=='placeholder-post.mdx');if(files.length!==1){console.error('Expected exactly 1 new MDX file (excluding placeholder), found:',files);process.exit(1)}const t=fs.readFileSync(`${dir}/${files[0]}`,'utf8');if(!t.includes('author: jon-van-loo')&&!t.includes('author: \"jon-van-loo\"'))process.exit(2);if(!t.includes('practiceArea: tax')&&!t.includes('practiceArea: \"tax\"'))process.exit(3);if(!t.includes('draft: true'))process.exit(4);console.log('OK',files[0])"</automated>
    <automated>npm run check</automated>
  </verify>
  <done>One new MDX file exists in `src/content/blog/` with the locked frontmatter (`author: jon-van-loo`, `practiceArea: tax`, `draft: true`, Jon-supplied title + slug + summary + publishedAt); `npm run check` passes (references resolve); the placeholder-post.mdx is UNTOUCHED (still draft:true, still present); the file body is empty or a single comment placeholder awaiting Jon's draft in Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (CHECKPOINT — D-14): Jon supplies the seed-post body text; Claude formats, runs lint:legal, surfaces hits, waits for sign-off</name>
  <files>src/content/blog/<seed-slug>.mdx</files>
  <action>
    This is a human-action checkpoint (D-14) — Claude PAUSES here. Per workflow.human_verify_mode=interactive, do NOT proceed to Task 3 until Jon types "publish" with a sign-off statement; if Jon types "hold" or "revise", loop back to the start of the checkpoint flow. The detailed Jon-facing flow is in the <what-built>, <how-to-verify>, and <resume-signal> blocks below.

    Action sequence for the executor:
    1. Display the <what-built> + <how-to-verify> message to Jon.
    2. Wait for Jon to paste body text.
    3. Format Jon's body into the existing MDX file (the one created in Task 1) using only headings, lists, links, and blockquotes (no Callouts / Citations / custom components per D-10).
    4. Run "npm run lint:legal" and surface any banned-term hits with the offending line + 1 line of context.
    5. Scan the body for client / counterparty / deal-value references; surface each for Jon's clearance authority.
    6. Display the formatted MDX + lint result + disclosure flags to Jon.
    7. Wait for the resume-signal: "publish" (proceed to Task 3), "revise" (loop to step 3 with Jon's revisions), or "hold" (stop the plan; post stays draft:true).
  </action>
  <verify>
    <human-check>Jon types "publish" with the sign-off statement confirming: (a) body is accurate and non-misleading (Rule 7.1); (b) any banned-term hits have been resolved (Rule 7.4); (c) all client / counterparty / deal-value references are in CLIENT_DISCLOSURE_CLEARANCE.md or have been cleared in this exchange; (d) no outcome-prediction language (LEGAL-10); (e) the blog disclaimer is sufficient for this body's risk profile (LEGAL-09).</human-check>
  </verify>
  <done>Jon's body is formatted into the MDX file; "npm run lint:legal" is green; disclosure flags are resolved; Jon has typed "publish" with the sign-off statement. Task 3 may now execute.</done>
  <what-built>
    Phase 5 infrastructure is complete and green:
    - 05-01: Wave 0 tests + package installs + Zod refine + rehype-external-links + og-default.svg
    - 05-02: buildArticleLd + BlogPostLayout chrome + [slug].astro author resolution + AuthorCard + .prose-bsv CSS
    - 05-03: FilterChipRow + /blog index chip rows + inline filter script
    - 05-04: rss.xml.ts endpoint via Container API + sanitize-html

    Task 1 has scaffolded one MDX file at `src/content/blog/<your-slug>.mdx` with the frontmatter ready (author: jon-van-loo, practiceArea: tax, draft: true). The body is empty.
  </what-built>
  <how-to-verify>
    **Step 1 — You paste your draft body text.**

    Paste it as your next message. It can be plain prose, can include lists, can include links to IRS bulletins / court opinions / SEC filings, and can include short code/citation blocks. I will format it into MDX (headings → `## `, lists → `- `, links → `[text](url)`) WITHOUT rewriting, paraphrasing, or adding examples. If your draft references a source, the source must already be in your draft — I won't add citations (D-02).

    **Step 2 — I do the formatting + lint:legal pass.**

    After you paste, I will:
    (a) Format your text into the MDX body (headings, lists, links, blockquotes only — no Callouts, no Citations, no custom components per D-10).
    (b) Run `npm run lint:legal` and surface any banned-term hits (Rule 7.4: 'expert', 'specialist', 'specialize'). If any hit fires, I show you the exact line + context and ask whether to rephrase or add to the allowlist (allowlist additions are documented per Phase 4 04-01).
    (c) Scan the body for client / counterparty / deal-value references. If I find any name or $-amount that looks like a client identifier, I list them and ask: "These names appear in your draft — confirm they're in `.planning/CLIENT_DISCLOSURE_CLEARANCE.md`, or tell me which to add (with your explicit clearance authority as the attorney of record)."

    **Step 3 — Your final sign-off.**

    After I show you the formatted MDX + lint:legal result + any disclosure flags, you have three options:
    (i) **"publish"** — I flip `draft: false`, delete `placeholder-post.mdx` in the same commit, build, push, and report the Vercel preview URL. (You also confirm in your sign-off message that the rendered draft is accurate, contains no outcome-prediction language (Rule 7.1 / LEGAL-10), and that any client references are cleared.)
    (ii) **"revise"** — You paste a revised body OR specific line changes; I re-format, re-lint, re-show.
    (iii) **"hold"** — Stop here. The post stays `draft: true`; we can resume any time by saying "resume Phase 5 publish."

    Per ABA Formal Opinion 477R and CA Rules of Professional Conduct 7.1 / 7.4, only you (Jon, the attorney of record) can confirm:
    - The body is accurate and non-misleading (Rule 7.1)
    - No banned-term hit slipped past lint:legal in a context the regex couldn't catch (Rule 7.4)
    - No client / counterparty / deal-value reference is uncleared
    - The post does not predict an outcome or guarantee a result (LEGAL-10)
    - The blog disclaimer rendered automatically by BlogPostLayout (LEGAL-09) is sufficient for THIS body's risk profile

    No automation can perform this review. This is the substantive control.
  </how-to-verify>
  <resume-signal>
    Paste the body text to begin. After formatting + lint:legal, respond with **"publish"** (with sign-off statement), **"revise"** (with revisions), or **"hold"**.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip draft:false, delete placeholder-post.mdx, update clearance register if needed, build, push, report preview URL (POST-CHECKPOINT only)</name>
  <files>src/content/blog/<seed-slug>.mdx, src/content/blog/placeholder-post.mdx, .planning/CLIENT_DISCLOSURE_CLEARANCE.md</files>
  <read_first>
    - src/content/blog/<seed-slug>.mdx (the file from Task 1 + Jon's body from Task 2)
    - src/content/blog/placeholder-post.mdx (verify it still exists and is draft:true — to be DELETED)
    - .planning/CLIENT_DISCLOSURE_CLEARANCE.md (current state — if Task 2 surfaced any new client/counterparty references, Jon's sign-off may include clearance for them; this task records them)
    - .planning/phases/05-insights-blog-system/05-RESEARCH.md § Pitfall 9 (lines 646-656 — same-commit deletion pattern; never an intermediate state with zero posts)
    - tests/blog-pages-exist.spec.ts (verify the un-skip condition matches — once draft:false flips, this test goes live and asserts exactly 1 published post)
  </read_first>
  <action>
    Triggered ONLY by Jon's **"publish"** response in Task 2. If Jon said "revise" or "hold," do NOT execute this task.

    Step A — Atomic state change (single commit):
    1. In the seed-post MDX, change the frontmatter line `draft: true` → `draft: false`.
    2. Delete `src/content/blog/placeholder-post.mdx` (Pitfall 9 — same commit as the publish).
    3. If Task 2 surfaced any new client / counterparty / deal-value references and Jon's sign-off included clearance for them, append a new entry to `.planning/CLIENT_DISCLOSURE_CLEARANCE.md` following the existing format (one bullet per item, with the date and Jon's name as authorising attorney). If Task 2 surfaced no such references, skip this step — do NOT touch the file.

    Step B — Build verification (pre-commit):
    Run, in order:
      - `npm run check` (Zod + TS)
      - `npm run lint:legal` (Rule 7.4 scanner; MUST exit 0)
      - `npm run build` (must succeed; produces `dist/client/blog/<seed-slug>/index.html` and an updated `dist/client/blog/rss.xml`)
      - `npm test` (full Playwright suite — the un-skipped specs from 05-01..05-04 now run real assertions against the published post)
    If any of these fail, STOP and surface the failure to Jon — do NOT commit.

    Step C — Commit + push:
    `git add` the three changed paths + the deleted placeholder. Commit with a single message in the project's existing style (see prior Phase 4 commit messages: `docs(05): publish Jon-authored crypto/blockchain tax seed post — flip draft:false, delete Phase 1 placeholder` or similar). Push to the current branch. Open a PR to `main` titled `Phase 5: publish Jon's seed Insights post + close phase`.

    Step D — Report:
    Once the Vercel preview deploys (usually under 90 seconds for an Astro build), report the preview URL to Jon plainly:
      "Phase 5 published. Preview: `https://bsv-new-website-<sha>.vercel.app/blog/<seed-slug>`. The post is live on the index at `.../blog`, the RSS feed at `.../blog/rss.xml` now carries one item, and the Article JSON-LD validates via Google's Rich Results Test (paste the URL into search.google.com/test/rich-results to confirm). When you merge the PR to `main`, production at `https://bsvlaw.com` (or the agreed transition domain — Phase 7 task) auto-deploys."

    Do NOT merge to main here — that's Jon's call (the merge is the Phase 5 closeout).

    DO NOT add the placeholder-post.mdx back. DO NOT re-add `reviewedBy` to the seed post frontmatter (D-13 forbids). DO NOT modify any test file in this task — the 05-01..05-04 tests are already configured to assert the published shape and to skip-gate when no posts exist.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(fs.existsSync('src/content/blog/placeholder-post.mdx')){console.error('placeholder-post.mdx still exists — must be deleted on publish commit');process.exit(1)}const dir='src/content/blog';const files=fs.readdirSync(dir).filter(f=>f.endsWith('.mdx'));if(files.length!==1){console.error('Expected exactly 1 blog MDX, found',files.length);process.exit(2)}const t=fs.readFileSync(`${dir}/${files[0]}`,'utf8');if(!t.includes('draft: false'))process.exit(3);if(t.includes('reviewedBy'))process.exit(4);console.log('OK published',files[0])"</automated>
    <automated>npm run lint:legal</automated>
    <automated>npm run build</automated>
    <automated>npm test</automated>
  </verify>
  <done>Exactly one MDX file in src/content/blog/ — the seed post — with `draft: false`; placeholder-post.mdx is deleted; CLIENT_DISCLOSURE_CLEARANCE.md updated only if Jon cleared new references; `npm run check` + `npm run lint:legal` + `npm run build` + `npm test` all green; PR opened against `main`; the Vercel preview URL is reported to Jon and points to a working `/blog/<seed-slug>` page with the full chrome, a working `/blog` index showing the post, and a working `/blog/rss.xml` carrying one item. Phase 5 success criteria #1-#4 (ROADMAP.md) all visibly true on the preview URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jon's draft text → MDX body | Content-fidelity rule (D-01): Claude formats only — does NOT rewrite or add. The substantive content review is Jon's. |
| MDX body → published HTML / RSS / Article JSON-LD | Same controls from 05-02 (BlogPostLayout-rendered disclaimer; rehype-external-links auto-rel; sanitize-html scrub in RSS) flow through automatically — no per-post wiring needed. |
| Client / counterparty / deal-value reference in body → public URL | Mitigated by the clearance register (LEGAL-04) + Jon's review at Task 2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | Pre-engagement client communications leak via post body | mitigate | Jon's substantive review at Task 2 IS the control. Content-fidelity rule prevents Claude from inventing references. Clearance register (LEGAL-04) is the ledger. Per-post disclaimer (LEGAL-09 / BLOG-03) automatically rendered by BlogPostLayout. |
| T-05-06 | Bar compliance | Misleading advertising / Rule 7.1 / 7.4 violation in post body | mitigate | Automated layer: `npm run lint:legal` scans the body for `expert` / `specialist` / `specialize` and fails the build on hits (Phase 4 04-01). Substantive layer: Jon's review at Task 2. Two-layer defense. |
| T-05-LEGAL | Bar compliance | Outcome-prediction or guarantee language ("we'll win your appeal") | accept (human gate) | Per Rule 7.1 / LEGAL-10, this is a substantive judgment call. Jon's sign-off at Task 2 explicitly states no outcome-prediction language. The blog disclaimer covers the "general informational purposes" framing structurally. |
</threat_model>

<verification>
After all three tasks complete (post-checkpoint):
- `src/content/blog/` contains exactly one MDX file — the seed post — with `draft: false` and Jon-authored body
- `src/content/blog/placeholder-post.mdx` is deleted (Pitfall 9 enforced)
- `.planning/CLIENT_DISCLOSURE_CLEARANCE.md` is updated only if Jon cleared new references; otherwise unchanged
- All five Phase 5 ROADMAP success criteria visibly true on the Vercel preview:
  1. ✓ Visitor lands on `/blog`, sees at least one published seed post by a named BSV attorney, and can filter by attorney + practice area (BLOG-05 / Slice 2)
  2. ✓ `/blog/<seed-slug>` renders author name link, per-post disclaimer, valid Article JSON-LD (BLOG-02..04 / Slice 1)
  3. ✓ Attempting to add a post without `author` fails the build (BLOG-02 / proven by tests/blog-zod-author.spec.ts from 05-01)
  4. ✓ `/blog/rss.xml` is a valid feed with the seed post item (BLOG-06 / Slice 3)
- `npm run lint:legal`, `npm run check`, `npm run build`, `npm test` all green
- Jon has reviewed the Vercel preview URL and confirmed sign-off
- A PR is open against main; merging it closes Phase 5 in ROADMAP.md (a follow-up Phase 5 SUMMARY step the orchestrator handles)
</verification>

<success_criteria>
- BLOG-09 closed: exactly one published (draft:false) post in src/content/blog/ — Jon-authored, jon-van-loo, practiceArea:tax.
- LEGAL-09 substantively closed: blog disclaimer rendered on the seed-post page (automatic via BlogPostLayout); lint:legal green; clearance register honored; Jon's review on record.
- The placeholder-post.mdx is gone in the same commit (Pitfall 9 enforced).
- All Phase 5 tests un-skipped in 05-01..05-04 now run real assertions and pass (article-jsonld validates the seed post's JSON-LD; blog-filter validates the chip-row DOM; rss-feed validates the seed post in the feed; blog-pages-exist validates `/blog/<seed-slug>/index.html` and the count-equals-1 invariant; disclaimer-crawl finds the blog disclaimer fragment on the seed-post page).
- Phase 5 production preview URL is reported to Jon and is fully functional.
</success_criteria>

<output>
Create `.planning/phases/05-insights-blog-system/05-05-SUMMARY.md` when done.
</output>
</content>
</invoke>