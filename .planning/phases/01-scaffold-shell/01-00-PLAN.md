---
phase: 01-scaffold-shell
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .gitleaks.toml
  - scripts/install-git-hooks.ps1
  - scripts/hooks/pre-commit
  - tests/disclaimer-crawl.spec.ts
  - tests/zod-negative.spec.ts
  - tests/disclaimer-set.spec.ts
  - tests/baselayout.spec.ts
  - tests/jsonld-legalservice.spec.ts
  - tests/gitleaks.spec.sh
  - tests/fixtures/broken-blog-post.mdx
  - playwright.config.ts
autonomous: true
requirements:
  - LEGAL-01
  - SEC-04
must_haves:
  truths:
    - "Test framework scaffolds exist before any npm install runs (no scavenger hunt for executors in later waves)"
    - ".gitleaks.toml exists with intake@bsvlaw.com allowlist so first-time gitleaks runs do not false-positive"
    - "Negative-test fixture (blog post with no author) exists, ready for Plan 02 to wire up"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright config pointing at astro preview; webServer command runs npm run build && npx astro preview"
      contains: "webServer"
    - path: ".gitleaks.toml"
      provides: "gitleaks rule set with BSV-specific allowlist"
      contains: "intake@bsvlaw.com"
    - path: "tests/disclaimer-crawl.spec.ts"
      provides: "Disclaimer crawl test scaffold (full implementation lands in Plan 07 once routes exist)"
    - path: "scripts/hooks/pre-commit"
      provides: "Pre-commit hook script that calls the gitleaks binary"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/*.spec.ts"
      via: "Playwright testDir config"
      pattern: "testDir.*tests"
threat_model:
  trust_boundaries:
    - "Local developer machine → committed git history (where pre-commit must catch secrets before push)"
  stride_register:
    - id: "T-1-SC"
      category: "Tampering"
      component: ".gitleaks.toml + scripts/hooks/pre-commit (file scaffolds; binary install in Plan 06)"
      disposition: "mitigate"
      mitigation: "Scaffold rule set and pre-commit hook script now so Plan 06 only has to install the binary and run the installer; allowlist intake@bsvlaw.com placeholder to prevent false positives in first run."
---

<objective>
Create test infrastructure scaffolds, gitleaks configuration files, and Playwright config BEFORE the first `npm install` runs in Plan 01. This is Wave 0: every file here is a contract that later waves implement against. Per VALIDATION.md, this plan closes the eight Wave 0 gaps so no `<verify>` block in a downstream plan is missing a target file.

Purpose: Eliminate the "scavenger hunt" anti-pattern where executors in later waves have to guess at test conventions. Every test file is created here as a runnable stub (skipped or pending), so Plan 07 can implement the bodies without inventing directory layout, configuration format, or fixture conventions.

Output:
- `playwright.config.ts` — Playwright config (stub points at astro preview on port 4321)
- `tests/*.spec.ts` — six test scaffold files (skipped pending Plan 07)
- `tests/fixtures/broken-blog-post.mdx` — D-24 negative-test fixture (stays out of `src/content/`)
- `tests/gitleaks.spec.sh` — shell test that attempts to commit `AKIAIOSFODNN7EXAMPLE`
- `.gitleaks.toml` — gitleaks rule set with BSV allowlist
- `scripts/hooks/pre-commit` — bash hook calling `gitleaks protect --staged`
- `scripts/install-git-hooks.ps1` — PowerShell installer Jon runs once
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-shell/01-CONTEXT.md
@.planning/phases/01-scaffold-shell/01-RESEARCH.md
@.planning/phases/01-scaffold-shell/01-VALIDATION.md
@.planning/phases/01-scaffold-shell/01-SKELETON.md
@.claude/CLAUDE.md

<rationale>
Plan 00 runs before `npm install` so it deliberately does NOT touch `package.json`, `package-lock.json`, or `node_modules/`. The Playwright config file is valid TypeScript that does not require Playwright to be installed to write or commit — installation lands in Plan 01's gated `npm install` step.

The six test spec files are scaffolded as `test.skip(...)` blocks so that when Plan 07 (and Plan 02 for the schema tests) lands, executors only have to flip `test.skip` to `test` and fill in the body — no scaffolding decisions get re-litigated.

The deliberately-broken blog fixture lives in `tests/fixtures/` (NOT `src/content/blog/`) so the regular content collection glob does NOT pick it up. The negative test copies the fixture into `src/content/blog/` at test time, asserts the build fails, then removes it.
</rationale>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Playwright config and six test spec files (all skipped)</name>
  <files>playwright.config.ts, tests/disclaimer-crawl.spec.ts, tests/zod-negative.spec.ts, tests/disclaimer-set.spec.ts, tests/baselayout.spec.ts, tests/jsonld-legalservice.spec.ts, tests/fixtures/broken-blog-post.mdx</files>
  <read_first>
    - .planning/phases/01-scaffold-shell/01-RESEARCH.md (sections "Pattern 8: Playwright disclaimer-crawl test" and "Wave 0 Gaps")
    - .planning/phases/01-scaffold-shell/01-VALIDATION.md (per-task verification map and Wave 0 list)
    - .planning/phases/01-scaffold-shell/01-SKELETON.md (test infrastructure section)
  </read_first>
  <action>
    Create `playwright.config.ts` at the repo root using the canonical config from RESEARCH.md §"Pattern 8":
    - Import `defineConfig` from `@playwright/test` (the package is not yet installed — that lands in Plan 01; the import is valid TypeScript regardless).
    - Set `testDir: './tests'`, `fullyParallel: false`, `forbidOnly: !!process.env.CI`, `workers: 1`.
    - `use.baseURL: 'http://localhost:4321'`.
    - `webServer.command: 'npm run build && npx astro preview'`, `webServer.url: 'http://localhost:4321'`, `webServer.timeout: 120_000`, `webServer.reuseExistingServer: !process.env.CI`.

    Create six test scaffold files. Every test inside each file is wrapped in `test.skip(...)` with a TODO comment naming the Plan number that will implement the body. The point is to commit runnable scaffolds, not implementations.

    File contents (exact):

    `tests/disclaimer-crawl.spec.ts`:
    - Import `test, expect, request` from `@playwright/test`.
    - Import `* as cheerio` from `cheerio`.
    - Single `test.describe('Disclaimer crawl', ...)` block with one `test.skip('footer disclaimer appears on every sitemap route — implementation lands in Plan 07', async () => { /* TODO: Plan 07 fills this in per RESEARCH.md §"Pattern 8" */ })`.
    - The skipped body MUST contain the canonical disclaimer-fragment constant: `const FOOTER_DISCLAIMER_FRAGMENT = 'The information on this website is for general informational purposes only';` This constant is the single source of truth used by both this test and by `src/content/disclaimers/disclaimers.json` (Plan 01 seeds the matching text). Defining it now prevents Plan 07 from inventing a different fragment.

    `tests/zod-negative.spec.ts`:
    - Skipped test: `test.skip('blog post with missing author fails build — implementation lands in Plan 02', async () => { /* TODO: Plan 02 copies tests/fixtures/broken-blog-post.mdx into src/content/blog/, runs npm run build, asserts non-zero exit + Zod error message in stderr, removes the file. */ })`.

    `tests/disclaimer-set.spec.ts`:
    - Skipped test: `test.skip('all five disclaimer ids exist with non-empty text + version — implementation lands in Plan 02', async () => { /* TODO: Plan 02 reads src/content/disclaimers/disclaimers.json, asserts entries with id in ['footer','contact','blog','practice-area','attorney'] each have non-empty text and a version string. */ })`.

    `tests/baselayout.spec.ts`:
    - Skipped test: `test.skip('BaseLayout renders firm name in header and disclaimer in footer — implementation lands in Plan 07', async () => { /* TODO: Plan 07 visits "/" via Playwright, asserts header contains "Belcher, Smolen & Van Loo LLP" (or "BSV Law"), asserts footer contains FOOTER_DISCLAIMER_FRAGMENT. */ })`.

    `tests/jsonld-legalservice.spec.ts`:
    - Skipped test: `test.skip('LegalService JSON-LD parses and contains firm name — implementation lands in Plan 07', async () => { /* TODO: Plan 07 visits "/", extracts <script type="application/ld+json">, JSON.parse it, asserts @type === "LegalService", name === "Belcher, Smolen & Van Loo LLP". */ })`.

    `tests/fixtures/broken-blog-post.mdx`:
    - Create a minimal MDX file with frontmatter intentionally MISSING the `author` field but present `title`, `slug`, `practiceArea`, `publishedAt`, `summary`, `reviewedBy`. Body is one paragraph. This file is the Plan 02 negative-test input (D-24).
    - Add a top-of-file comment: `{/* DO NOT add an `author` field to this file. This fixture proves Zod rejects missing author. Plan 02's zod-negative test copies it into src/content/blog/, runs build, asserts failure, removes it. */}`

    Do NOT install Playwright or Cheerio in this plan — that gates behind the Plan 01 checkpoint:human-verify.

    `tests/fixtures/` directory MUST NOT be picked up by any future content-collection glob; Plan 02's `src/content.config.ts` writes globs scoped to `./src/content/<collection>/` exactly, so this directory is naturally outside.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$files = @('playwright.config.ts','tests/disclaimer-crawl.spec.ts','tests/zod-negative.spec.ts','tests/disclaimer-set.spec.ts','tests/baselayout.spec.ts','tests/jsonld-legalservice.spec.ts','tests/fixtures/broken-blog-post.mdx'); $missing = $files | Where-Object { -not (Test-Path $_) }; if ($missing) { Write-Error \"Missing: $($missing -join ', ')\"; exit 1 }; if ((Get-Content tests/disclaimer-crawl.spec.ts -Raw) -notmatch 'FOOTER_DISCLAIMER_FRAGMENT') { Write-Error 'Disclaimer fragment constant missing'; exit 1 }; if ((Get-Content tests/fixtures/broken-blog-post.mdx -Raw) -match '^author:|\nauthor:') { Write-Error 'broken-blog-post.mdx MUST NOT have author field'; exit 1 }; Write-Host 'All Wave 0 test scaffolds present and well-formed'"</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` exists at repo root.
    - All six files under `tests/` and `tests/fixtures/` exist.
    - Every `.spec.ts` file uses `test.skip(...)` for its test body (no unskipped tests at this stage — the test command in Plan 01 onward must not fail because of test scaffolds).
    - `tests/disclaimer-crawl.spec.ts` contains the literal string `FOOTER_DISCLAIMER_FRAGMENT` and the literal disclaimer sentence `The information on this website is for general informational purposes only`.
    - `tests/fixtures/broken-blog-post.mdx` does NOT contain an `author:` frontmatter line (verifies the negative-test premise).
    - No `package.json` or `node_modules/` is created or modified by this task.
  </acceptance_criteria>
  <done>All scaffold files exist; the Playwright config is syntactically valid TypeScript; the broken-blog-post fixture is missing the `author` field as required by D-24.</done>
</task>

<task type="auto">
  <name>Task 2: Create gitleaks config, pre-commit hook script, and gitleaks shell test</name>
  <files>.gitleaks.toml, scripts/hooks/pre-commit, scripts/install-git-hooks.ps1, tests/gitleaks.spec.sh</files>
  <read_first>
    - .planning/phases/01-scaffold-shell/01-RESEARCH.md (sections "Pattern 10: `.gitleaks.toml` and pre-commit hook" and "Pitfall A1")
    - .planning/phases/01-scaffold-shell/01-CONTEXT.md (D-17, D-18 — secret-scanning two-layer setup)
    - .claude/CLAUDE.md (security non-negotiables)
  </read_first>
  <action>
    Create `.gitleaks.toml` at repo root with this exact content (per RESEARCH.md §"Pattern 10"):
    - `[extend]` block with `useDefault = true` (inherits gitleaks's built-in rule set).
    - `[allowlist]` block with `description = "BSV Law project allowlist"`, `paths = ['''src/content/disclaimers/disclaimers\.json''', '''src/content/attorneys/placeholder-attorney\.mdx''']`, `regexes = ['''intake@bsvlaw\.com''']`.
    - Justification for the allowlist entries lives in a comment block at the top — name the decision IDs (D-17 for the two-layer scan, "Claude's discretion" in CONTEXT.md for the intake email allowance).

    Create `scripts/hooks/pre-commit` (POSIX shell, Unix line endings). Per RESEARCH.md §"Pattern 10":
    - Shebang `#!/usr/bin/env bash`.
    - `set -e`.
    - `echo "Running gitleaks pre-commit check..."`.
    - `gitleaks protect --staged --no-banner --redact -v`.

    The hook calls the `gitleaks` binary directly (NOT `npx gitleaks` — per RESEARCH.md Pitfall A1, the npm `gitleaks@1.0.0` package is a slopsquat). The binary install lands in Plan 06's checkpoint:human-verify task.

    Create `scripts/install-git-hooks.ps1` per RESEARCH.md §"Pattern 10":
    - Resolve repo root with `git rev-parse --show-toplevel`.
    - Copy `scripts/hooks/pre-commit` to `.git/hooks/pre-commit`.
    - Set executable permissions via `icacls $hookPath /grant Everyone:RX`.
    - Echo success message with the hook path.

    Create `tests/gitleaks.spec.sh` (bash script, NOT a Playwright test — the `.sh` extension matches VALIDATION.md). Per VALIDATION.md row `01-05-05`:
    - Shebang `#!/usr/bin/env bash`.
    - `set -e`.
    - Create a throwaway branch `gitleaks-negative-test-$(date +%s)`.
    - Stage a temp file containing the fake AWS key `AKIAIOSFODNN7EXAMPLE`.
    - Run `git commit -m "negative test"` and assert it EXITS NON-ZERO (gitleaks blocks the commit).
    - Clean up: `git reset --hard` and `git branch -D` the temp branch.
    - Echo "PASS: gitleaks blocked AWS key commit" on success; non-zero exit on failure.
    - Add a header comment noting this script is run manually in Plan 06's verification step (gitleaks binary required); CI runs the equivalent via the GitHub Actions gitleaks step in Plan 07.

    Use `git rev-parse --show-toplevel`-relative paths in all four scripts so they work regardless of the current working directory.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$files = @('.gitleaks.toml','scripts/hooks/pre-commit','scripts/install-git-hooks.ps1','tests/gitleaks.spec.sh'); $missing = $files | Where-Object { -not (Test-Path $_) }; if ($missing) { Write-Error \"Missing: $($missing -join ', ')\"; exit 1 }; $toml = Get-Content .gitleaks.toml -Raw; if ($toml -notmatch 'useDefault = true' -or $toml -notmatch 'intake@bsvlaw\\.com') { Write-Error '.gitleaks.toml malformed'; exit 1 }; $hook = Get-Content scripts/hooks/pre-commit -Raw; if ($hook -notmatch 'gitleaks protect --staged' -or $hook -match 'npx gitleaks') { Write-Error 'pre-commit hook calls wrong gitleaks (must be binary, not npx)'; exit 1 }; $shtest = Get-Content tests/gitleaks.spec.sh -Raw; if ($shtest -notmatch 'AKIAIOSFODNN7EXAMPLE') { Write-Error 'gitleaks shell test does not use AKIAIOSFODNN7EXAMPLE'; exit 1 }; Write-Host 'gitleaks scaffolds OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitleaks.toml` exists with `useDefault = true` and contains the literal `intake@bsvlaw.com` allowlist entry.
    - `scripts/hooks/pre-commit` exists, contains `gitleaks protect --staged`, and does NOT contain the string `npx gitleaks` (Pitfall A1 enforcement).
    - `scripts/install-git-hooks.ps1` exists with a `Copy-Item` line targeting `.git/hooks/pre-commit`.
    - `tests/gitleaks.spec.sh` exists and contains the literal `AKIAIOSFODNN7EXAMPLE` AWS key fragment.
    - No actual `gitleaks` binary install happens here (that's gated in Plan 06).
  </acceptance_criteria>
  <done>All four files exist with the verified contents. The pre-commit hook script calls the gitleaks binary directly, not via npm/npx, per RESEARCH.md Pitfall A1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local working tree → committed git history | Plan 00 lays the groundwork for Plan 06's gitleaks pre-commit hook to catch secrets BEFORE they cross this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC-W0 | Tampering (supply chain) | `tests/*.spec.ts` files using imports from `@playwright/test` and `cheerio` | mitigate | Scaffold files reference the packages but do not run them. Plan 01's `checkpoint:human-verify` gate runs BEFORE the first `npm install`, so this plan cannot pull a malicious version. The version pins (Playwright 1.60.0, Cheerio 1.2.0) come from RESEARCH.md's Package Legitimacy Audit. |
| T-1-Secrets-Scaffold | Information Disclosure | `tests/gitleaks.spec.sh` containing `AKIAIOSFODNN7EXAMPLE` | accept | `AKIAIOSFODNN7EXAMPLE` is the AWS documentation example key — not a real credential. It is explicitly allowlisted by gitleaks's default rules as a known-test fingerprint, so committing it does NOT trip the pre-commit hook (verified by RESEARCH.md research). The test file itself is the negative test — its presence in the repo is intentional. |
</threat_model>

<verification>
- All seven scaffold files exist in their declared locations.
- `playwright.config.ts` is syntactically valid TypeScript (passes a `tsc --noEmit` check once Plan 01 installs TypeScript).
- `.gitleaks.toml` is parseable TOML.
- `scripts/install-git-hooks.ps1` is syntactically valid PowerShell (passes `powershell -Command "Get-Command -Syntax"` once written).
- No `package.json`, no `node_modules`, no Astro files exist yet — Plan 01 owns those.
</verification>

<success_criteria>
- VALIDATION.md Wave 0 row count drops from 12 missing files to 4 (the remaining four — `.github/workflows/ci.yml`, plus three test bodies — land in Plans 01 and 07).
- Every test scaffold uses `test.skip(...)` so a future `npm run test:disclaimer` after Plan 01 runs cleanly (no skipped-test errors fail the pipeline).
- gitleaks config + pre-commit hook script + installer + negative-test shell script are all committed, ready for Plan 06 to wire up.
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-shell/01-00-SUMMARY.md` when done, listing the files created and noting that Plan 01 is unblocked to begin the Walking Skeleton scaffold.
</output>
