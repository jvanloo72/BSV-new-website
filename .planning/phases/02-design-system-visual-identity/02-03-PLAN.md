---
phase: 02-design-system-visual-identity
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/icons/mark.svg
  - public/favicon.svg
  - src/components/chrome/SiteHeader.astro
  - src/components/chrome/SiteFooter.astro
  - src/pages/_design.astro
autonomous: true
requirements: [DESIGN-04, DESIGN-06, DESIGN-07, DESIGN-08]
must_haves:
  truths:
    - "A small derived mark (from the converging-lines motif) exists and is used as the favicon and in the site chrome"
    - "SiteHeader shows the typographic wordmark + mark; nav links have animated accent underline + focus-visible rings"
    - "SiteFooter renders the refined treatment AND still renders the site-wide <Disclaimer id=\"footer\"> on every route (LEGAL-01 not regressed)"
    - "Phase 1 chrome's arbitrary-value token syntax (text-[color:var(...)]) is retrofitted to the namespace utilities (text-text, border-border, bg-bg)"
  artifacts:
    - path: "src/icons/mark.svg"
      provides: "DESIGN-06 derived wordmark mark (currentColor, reads at 16-32px)"
      contains: "currentColor"
    - path: "public/favicon.svg"
      provides: "updated favicon from the new mark"
    - path: "src/components/chrome/SiteHeader.astro"
      provides: "refined header — wordmark + mark, namespace utilities, animated underline, focus rings"
      contains: "border-border"
    - path: "src/components/chrome/SiteFooter.astro"
      provides: "refined footer — mark, both offices, intake link, intact <Disclaimer>"
      contains: "Disclaimer"
  key_links:
    - from: "src/components/chrome/SiteHeader.astro"
      to: "src/icons/mark.svg"
      via: "<Icon name=\"mark\"> at compact sizes"
      pattern: "mark"
    - from: "src/components/chrome/SiteFooter.astro"
      to: "src/components/legal/Disclaimer.astro"
      via: "<Disclaimer id=\"footer\" /> (must not regress)"
      pattern: "Disclaimer id=\"footer\""
---

<objective>
Land the firm's visual identity into the site chrome and finish the token-syntax standardization. Author the
small derived mark (from the converging-lines hero motif), wire it as the favicon and into SiteHeader/SiteFooter,
replace Phase 1's plain text firm name with the typographic Hanken wordmark, add the animated accent nav
underline + focus-visible rings, and retrofit the two chrome files from the Phase 1 arbitrary-value token form
(`text-[color:var(--color-text)]`) to the Phase 2 namespace utilities (`text-text`, `border-border`, `bg-bg`).
Critically, the SiteFooter refinement must NOT regress the site-wide `<Disclaimer id="footer" />` (LEGAL-01 —
the disclaimer-crawl test asserts it on every route).

Purpose: D-14 wordmark/mark + D-27/D-28 chrome refinement; finalize the namespace-syntax standardization
(UI-SPEC §258) so the whole codebase is consistent and a future restyle stays a single-file edit.
Output: mark.svg + updated favicon; refined SiteHeader + SiteFooter; the gallery showing the chrome treatment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-visual-identity/02-UI-SPEC.md
@.planning/phases/02-design-system-visual-identity/02-PATTERNS.md
@.planning/phases/02-design-system-visual-identity/02-01-SUMMARY.md
@.planning/phases/02-design-system-visual-identity/02-02-SUMMARY.md

<interfaces>
<!-- Current chrome to refactor (extracted from the codebase). -->
SiteHeader.astro: Props { currentPath?: string }; navLinks[] mapped to <li><a>. Currently uses
  `border-b border-[color:var(--color-border)] bg-[color:var(--color-bg)]`,
  `text-[color:var(--color-text)]`, `text-[color:var(--color-text-muted)]`, plain text firm name
  "Belcher, Smolen & Van Loo LLP". aria-current on the active link (line 38) — KEEP.
SiteFooter.astro: imports Disclaimer + SITE; renders `<Disclaimer id="footer" />` (line 10 — KEEP),
  both offices ("Silicon Valley · 555 California St., Suite 4925, San Francisco, CA 94104"), mailto SITE.email,
  copyright. Container `mx-auto max-w-6xl px-6` (KEEP — locked content width). Uses
  `border-t border-[color:var(--color-border)] bg-[color:var(--color-bg-elevated)]` and muted text arbitrary form.
astro-icon: `import { Icon } from 'astro-icon/components';` → `<Icon name="mark" class="..." />`.
Token utilities available (from 02-01 global.css): text-text, text-text-muted, text-accent, bg-bg,
  bg-bg-elevated, border-border, focus-visible:ring-accent. Animated underline idiom (UI-SPEC §Motion):
  `relative` + `after:h-px after:w-0 after:bg-accent motion-safe:after:transition-[width] hover:after:w-full`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author mark.svg + update favicon</name>
  <files>src/icons/mark.svg, public/favicon.svg</files>
  <read_first>
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §"Creative Art Contract" (derived wordmark mark row) / §D-14
    - .planning/phases/02-design-system-visual-identity/02-PATTERNS.md §"Asset Files (SVG)" / §"No Analog Found"
    - .planning/phases/02-design-system-visual-identity/02-RESEARCH.md §"Open Questions" (Q3 mark form) / §"Pattern 5 (currentColor)"
    - src/assets/illustrations/hero-deal-flow.svg (02-02 — derive the mark from this motif) ; public/favicon.svg (current)
  </read_first>
  <action>
    Author `src/icons/mark.svg` — a compact glyph DERIVED from the converging-lines hero motif (D-14, a
    modernization not a rebrand): same thin-line system, `viewBox="0 0 48 48" fill="none" stroke="currentColor"`,
    must read clearly at 16–32px (simplify the hero into a single recognizable convergence glyph). currentColor
    only, ≤ 200 KB (expect < 2 KB). Update `public/favicon.svg` to the new mark (favicon is a standalone file
    referenced by BaseLayout's existing `<link rel="icon" href="/favicon.svg">`; it may carry an explicit accent
    color since it is not theme-following). Keep dimensions/viewBox favicon-appropriate.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/icons/mark.svg exists, currentColor only, ≤ 200 KB, derived from the converging-lines motif
    - public/favicon.svg updated to the new mark and ≤ 200 KB
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Mark + favicon authored; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Refine SiteHeader + SiteFooter; retrofit token syntax; keep Disclaimer intact</name>
  <files>src/components/chrome/SiteHeader.astro, src/components/chrome/SiteFooter.astro, src/pages/_design.astro</files>
  <read_first>
    - src/components/chrome/SiteHeader.astro, src/components/chrome/SiteFooter.astro (full current files)
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §"Refined chrome" / §"Motion & Interaction Contract"
    - .planning/phases/02-design-system-visual-identity/02-PATTERNS.md §SiteHeader.astro / §SiteFooter.astro / §"Critical Cross-Cutting Convention" / §"Disclaimer composition (do-not-regress)"
    - src/components/legal/Disclaimer.astro (the component that must keep rendering) ; tests/disclaimer-crawl.spec.ts (the test that enforces it)
    - src/icons/mark.svg (Task 1)
  </read_first>
  <action>
    SiteHeader: retrofit `border-b border-[color:var(--color-border)] bg-[color:var(--color-bg)]` →
    `border-b border-border bg-bg`; `text-[color:var(--color-text)]` → `text-text`;
    `text-[color:var(--color-text-muted)]` → `text-text-muted`. Replace the plain text firm name with the
    typographic wordmark (Hanken, weight 700/800, tracked) accompanied by `<Icon name="mark" ... />` at compact
    sizes. Add the animated accent underline to nav links (`after:` pseudo + `motion-safe:after:transition-[width]`)
    and `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent`. KEEP the `navLinks` array,
    the `.map()` render, and the `aria-current` active-state logic (do not regress).
    SiteFooter: retrofit `border-t border-[color:var(--color-border)] bg-[color:var(--color-bg-elevated)]` →
    `border-t border-border bg-bg-elevated` and the muted text class to `text-text-muted`. Add the `mark.svg`.
    KEEP `import Disclaimer`, `import { SITE }`, the `<Disclaimer id="footer" />` render (LEGAL-01 — MUST NOT
    REGRESS), both office strings, the mailto SITE.email intake link, the copyright, and the
    `mx-auto max-w-6xl px-6` container.
    Add a "Chrome" labeled section to `src/pages/_design.astro` (or confirm header/footer already render via
    BaseLayout) so the refined chrome is visible in the gallery — without breaking the existing 8 data-component
    markers or the noindex prop.
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/disclaimer-crawl.spec.ts tests/gallery.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - SiteHeader + SiteFooter use ONLY namespace utilities (no remaining `text-[color:var(...)]` / `border-[color:var(...)]` / `bg-[color:var(...)]` in either file)
    - SiteHeader shows the wordmark + mark, animated nav underline, focus-visible accent ring, and keeps aria-current
    - SiteFooter still renders `<Disclaimer id="footer" />`, both offices, the intake mailto, and the mark
    - tests/disclaimer-crawl.spec.ts passes (footer disclaimer on every route — NOT regressed)
    - tests/gallery.spec.ts still passes (8 markers intact); `npm run build` green
  </acceptance_criteria>
  <done>Refined chrome shipped; disclaimer-crawl + gallery green; syntax retrofit complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → browser (CSP) | Chrome refinement adds no remote asset and no script; CSP report-only stays clean. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering (XSS) | mark.svg / favicon | mitigate | Hand-authored line-art SVG, no embedded script; astro-icon runs SVGO on the inlined mark. |
| T-02-05 | Repudiation/Compliance | SiteFooter disclaimer | mitigate | LEGAL-01 footer disclaimer must render on every route; do-not-regress enforced by tests/disclaimer-crawl.spec.ts in this plan's verify. |
</threat_model>

<verification>
- `npm run build` green.
- `npm run test` (full Phase 2 + Phase 1 disclaimer suite) green — especially tests/disclaimer-crawl.spec.ts (LEGAL-01 not regressed) and tests/gallery.spec.ts (8 markers intact).
- No `text-[color:var(...)]` / `border-[color:var(...)]` / `bg-[color:var(...)]` remains in the two chrome files.
</verification>

<success_criteria>
The firm's wordmark + derived mark are live in the header, footer, and favicon; chrome uses the refined,
namespace-utility token syntax; the site-wide footer disclaimer still renders on every route. The entire
codebase now uses one token syntax and the Phase 2 design system is complete and reviewable on a Vercel
preview before Phase 3.
</success_criteria>

<output>
Create `.planning/phases/02-design-system-visual-identity/02-03-SUMMARY.md` when done.
</output>
