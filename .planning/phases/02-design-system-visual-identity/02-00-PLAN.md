---
phase: 02-design-system-visual-identity
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/design-tokens.spec.ts
  - tests/fonts-selfhost.spec.ts
  - tests/gallery.spec.ts
  - tests/assets-budget.spec.ts
  - tests/contrast.spec.ts
  - tests/a11y-interactions.spec.ts
  - tests/design-route-hidden.spec.ts
  - package.json
autonomous: true
requirements: [DESIGN-02, DESIGN-03, DESIGN-04, DESIGN-05, DESIGN-06]
must_haves:
  truths:
    - "Seven Wave-0 spec files exist and are runnable (skipped where their target does not yet exist)"
    - "tests/contrast.spec.ts asserts the 5 WCAG pairings now (pure math, no build dependency) and passes"
    - "package.json has a test:<name> script for each new spec"
  artifacts:
    - path: "tests/contrast.spec.ts"
      provides: "A11Y-04 WCAG contrast assertions (active, not skipped)"
      contains: "9A3F1A"
    - path: "tests/design-tokens.spec.ts"
      provides: "DESIGN-02 compiled-CSS token presence (skipped until tokens land)"
    - path: "tests/gallery.spec.ts"
      provides: "DESIGN-04/06 gallery component+icon presence (skipped until /_design lands)"
    - path: "tests/assets-budget.spec.ts"
      provides: "DESIGN-05/06 + PERF-02 asset existence + ≤200KB (skipped until assets land)"
    - path: "tests/fonts-selfhost.spec.ts"
      provides: "DESIGN-03 self-hosted font, no CDN URL (skipped until font lands)"
    - path: "tests/a11y-interactions.spec.ts"
      provides: "D-10/A11Y-03 keyboard + reduced-motion (skipped until components land)"
    - path: "tests/design-route-hidden.spec.ts"
      provides: "D-16 noindex + sitemap exclusion (skipped until /_design lands)"
  key_links:
    - from: "tests/*.spec.ts"
      to: "package.json scripts"
      via: "test:<name> playwright invocation"
      pattern: "playwright test tests/"
---

<objective>
Create the Wave-0 test scaffolding for Phase 2: seven Playwright/Cheerio/filesystem spec files
that encode the phase's automated verification contract, plus matching `package.json` test scripts.
Tests whose target does not yet exist are written with `test.describe.skip(...)` (or per-test `test.skip`)
and carry a `// UNSKIP WHEN: <condition>` comment so later plans un-skip them as the target ships.
The one test that needs no build target — `tests/contrast.spec.ts` (pure WCAG math on the locked hexes) —
is written ACTIVE and must pass immediately.

Purpose: Satisfy the Nyquist requirement (VALIDATION.md) — every DESIGN-0x behavior has an automated
verify that exists before the implementation it checks. This is the "create the test scaffold first" step.
Output: 7 spec files + updated package.json scripts; `nyquist_compliant` becomes satisfiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-visual-identity/02-VALIDATION.md
@.planning/phases/02-design-system-visual-identity/02-UI-SPEC.md
@.planning/phases/02-design-system-visual-identity/02-PATTERNS.md

<interfaces>
<!-- Phase 1 test idioms the executor MUST mirror. Extracted from the codebase. -->
From tests/disclaimer-crawl.spec.ts (Idiom A — build-first + dist disk read + cheerio):
```ts
import { test, expect } from '@playwright/test';
import { execSync } from 'node:child_process';
import * as fs from 'node:fs';
import * as cheerio from 'cheerio';
test.beforeAll(() => { execSync('npm run build', { stdio: 'pipe' }); });
// reads dist/client/<path>.html, then cheerio.load(html), asserts via $('selector')
```
From tests/disclaimer-set.spec.ts (Idiom B — filesystem / pure computation, no build):
```ts
import { test, expect } from '@playwright/test';
import * as fs from 'node:fs';
// reads source files / fs.statSync for sizes / computes in-test
```
NEVER use `astro preview` (Vercel-adapter incompatible; playwright.config.ts gates webServer behind
PLAYWRIGHT_NEEDS_SERVER, default off). Build writes to `dist/client/`.
package.json existing script pattern (lines 15-19): `"test:baselayout": "playwright test tests/baselayout.spec.ts"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: contrast.spec.ts (ACTIVE) + assets-budget.spec.ts + design-tokens.spec.ts</name>
  <files>tests/contrast.spec.ts, tests/assets-budget.spec.ts, tests/design-tokens.spec.ts, package.json</files>
  <read_first>
    - tests/disclaimer-set.spec.ts (Idiom B — filesystem + pure assertions; analog for contrast + assets-budget)
    - tests/jsonld-legalservice.spec.ts (Idiom A — build + dist grep; analog for design-tokens)
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §"WCAG contrast" (the 5 pairings + exact expected ratios) and §"Color Tokens" (locked hexes)
    - .planning/phases/02-design-system-visual-identity/02-VALIDATION.md (Per-Task Verification Map rows for DESIGN-02, A11Y-04, PERF-02)
    - package.json (existing test:<name> script style)
  </read_first>
  <action>
    Create `tests/contrast.spec.ts` ACTIVE (no skip — pure math, no build): implement the WCAG 2.1
    relative-luminance contrast formula in-test, then assert these locked pairings each meet their floor:
    text #111111 on bg #F8F5F0 ≥ 4.5 (expect ≈17.36); text-muted #52524E on bg #F8F5F0 ≥ 4.5 (≈7.22);
    text #111111 on bg-elevated #FDFCFA ≥ 4.5; text-muted #52524E on bg-elevated #FDFCFA ≥ 4.5 (≈7.65);
    accent #9A3F1A on bg #F8F5F0 ≥ 4.5 (≈6.23); accent #9A3F1A on bg-elevated #FDFCFA ≥ 4.5 (≈6.61);
    accent-fg #F8F5F0 on accent #9A3F1A ≥ 4.5 (≈6.23). border #D9D2C5 on bg is decorative — do NOT
    gate it (assert nothing, or document it as exempt). Covers A11Y-04.
    Create `tests/assets-budget.spec.ts` with `test.describe.skip` wrapping the asset-existence checks
    (the SVGs/woff2/headshots do not exist yet) BUT make the PERF-02 size-scan loop tolerant: use Idiom B,
    `fs.statSync` over src/assets/, src/icons/, public/ for image+font extensions, assert each ≤ 204800 bytes.
    Add a `// UNSKIP WHEN: hero SVG + 3 practice icons + woff2 committed (02-01/02-02)` comment. Covers DESIGN-05/06, PERF-02.
    Create `tests/design-tokens.spec.ts` as Idiom A wrapped in `test.describe.skip` with
    `// UNSKIP WHEN: global.css @theme rewritten (02-01)`: build, then grep `dist/_astro/*.css` for the
    lowercased locked hexes `#9a3f1a` and `#f8f5f0` (both PRESENT). Covers DESIGN-02.
    In `package.json`, add `test:contrast`, `test:assets-budget`, `test:design-tokens` scripts following
    the existing `playwright test tests/<name>.spec.ts` pattern.
  </action>
  <verify>
    <automated>npx playwright test tests/contrast.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/contrast.spec.ts runs ACTIVE and passes (all 7 text/icon/button pairings ≥ 4.5:1)
    - tests/assets-budget.spec.ts and tests/design-tokens.spec.ts exist, are skipped, carry an UNSKIP-WHEN comment
    - package.json has test:contrast, test:assets-budget, test:design-tokens scripts
    - `npx playwright test tests/contrast.spec.ts` exits 0
  </acceptance_criteria>
  <done>contrast.spec passes; the two skipped specs and their scripts exist.</done>
</task>

<task type="auto">
  <name>Task 2: gallery / a11y-interactions / design-route-hidden / fonts-selfhost specs (skipped) + scripts</name>
  <files>tests/gallery.spec.ts, tests/a11y-interactions.spec.ts, tests/design-route-hidden.spec.ts, tests/fonts-selfhost.spec.ts, package.json</files>
  <read_first>
    - tests/disclaimer-crawl.spec.ts (Idiom A; reusable readSitemapUrls()/urlToFilePath() helpers; dist disk reads)
    - tests/jsonld-legalservice.spec.ts (Idiom A build + dist grep — analog for fonts-selfhost)
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §"Component Inventory" (the 8 data-component markers) and §"Hidden Gallery Route" (noindex + sitemap)
    - .planning/phases/02-design-system-visual-identity/02-PATTERNS.md §"Test Files (Wave 0)" (exact per-test target + selector guidance)
    - .planning/phases/02-design-system-visual-identity/02-VALIDATION.md (rows for DESIGN-04/06, A11Y-03/D-10, D-16, DESIGN-03)
  </read_first>
  <action>
    All four are Idiom A, each wrapped in `test.describe.skip` with an `// UNSKIP WHEN:` comment.
    `tests/gallery.spec.ts` (`// UNSKIP WHEN: /_design renders all 8 components — 02-01 for PracticeAreaCard, 02-02 for the rest`):
    build, read `dist/client/_design/index.html`, cheerio-assert AT LEAST ONE element with
    `[data-component="X"]` for each of Hero, PracticeAreaCard, AttorneyCard, TestimonialQuote, DealsGrid,
    FeeStructureBand, CtaBlock, FaqAccordion (presence per component name — the gallery intentionally
    renders multiple PracticeAreaCards (≥3: M&A/IP/Tax) and AttorneyCards (≥5), so assert count ≥1 for
    each name, NOT exactly one), and assert ≥3 inline `<svg>` (the practice icons). Covers DESIGN-04/06.
    `tests/a11y-interactions.spec.ts` (`// UNSKIP WHEN: FaqAccordion + interactive components on /_design — 02-02`):
    cheerio-assert `<details>` present in `_design` HTML, at least one `motion-safe:` class present, and
    `focus-visible:ring` present on interactive elements. Covers A11Y-03/D-10.
    `tests/design-route-hidden.spec.ts` (`// UNSKIP WHEN: /_design route + sitemap filter live — 02-01`):
    build, grep `dist/client/sitemap-0.xml` for `_design` (must be ZERO occurrences — reuse disclaimer-crawl's
    sitemap read), and grep `dist/client/_design/index.html` for `noindex` (must be PRESENT). Covers D-16.
    `tests/fonts-selfhost.spec.ts` (`// UNSKIP WHEN: Hanken Grotesk wired via Astro Fonts API — 02-01`):
    build, grep ALL of `dist/` for `fonts.googleapis` and `fonts.gstatic` (must be ZERO), and assert
    `font-display` and `@font-face` appear in a compiled CSS file under `dist/`. Covers DESIGN-03.
    Add `test:gallery`, `test:a11y-interactions`, `test:design-route-hidden`, `test:fonts-selfhost`
    scripts to package.json in the existing style.
  </action>
  <verify>
    <automated>npx playwright test tests/gallery.spec.ts tests/a11y-interactions.spec.ts tests/design-route-hidden.spec.ts tests/fonts-selfhost.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four spec files exist, are skipped, and each has an UNSKIP-WHEN comment naming the unblocking plan
    - Running the four specs reports them as skipped (0 failures)
    - package.json has the four matching test:<name> scripts
    - No spec uses `astro preview` or a webServer; all read dist/ from disk
  </acceptance_criteria>
  <done>Four skipped Idiom-A specs + scripts exist; suite reports skips, not failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 2 test scaffolding produces only local test files; no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs | accept | This plan installs NOTHING (Playwright + Cheerio already present from Phase 1). No package-manager activity → supply-chain surface unchanged. The astro-icon/sharp install is gated in 02-01. |
| T-02-01 | Information Disclosure | tests reading dist/ | accept | Tests read build output on the local FS only; emit no secrets. No live boundary. |
</threat_model>

<verification>
- `npx playwright test tests/contrast.spec.ts` passes (active).
- The other six specs report as skipped (not failed) when run.
- `npm run test` runs the whole suite without errors (skips counted, not failures).
</verification>

<success_criteria>
All 7 Wave-0 spec files exist; contrast.spec is active and green; the other 6 are skipped with
UNSKIP-WHEN markers; package.json carries a script per spec. Phase 2 Nyquist coverage is now in place
for later plans to un-skip as targets ship.
</success_criteria>

<output>
Create `.planning/phases/02-design-system-visual-identity/02-00-SUMMARY.md` when done.
</output>
