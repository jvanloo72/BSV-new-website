---
phase: 02-design-system-visual-identity
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/Hero.astro
  - src/components/sections/AttorneyCard.astro
  - src/components/sections/TestimonialQuote.astro
  - src/components/sections/DealsGrid.astro
  - src/components/sections/FeeStructureBand.astro
  - src/components/sections/CtaBlock.astro
  - src/components/sections/FaqAccordion.astro
  - src/assets/illustrations/hero-deal-flow.svg
  - src/icons/practice-ip-tech.svg
  - src/icons/practice-tax.svg
  - src/assets/headshots/
  - src/pages/_design.astro
  - tests/gallery.spec.ts
  - tests/a11y-interactions.spec.ts
  - tests/assets-budget.spec.ts
autonomous: true
requirements: [DESIGN-04, DESIGN-05, DESIGN-06, DESIGN-07, DESIGN-08]
must_haves:
  truths:
    - "/_design renders all 8 locked components (Hero, PracticeAreaCard, AttorneyCard, TestimonialQuote, DealsGrid, FeeStructureBand, CtaBlock, FaqAccordion) with realistic FIRM_BRIEF placeholder content"
    - "The converging-linework hero SVG and all 3 thin-line practice-area icons exist on disk, each ≤200KB, themed via currentColor"
    - "FaqAccordion expands/collapses with ZERO JavaScript (native <details>) and is keyboard-operable"
    - "No component hardcodes hex; no client:* directive appears anywhere"
  artifacts:
    - path: "src/components/sections/Hero.astro"
      provides: "Hero with --text-display H1, inlined decorative hero SVG, composed Button CTAs"
      contains: "data-component=\"Hero\""
    - path: "src/components/sections/FaqAccordion.astro"
      provides: "JS-free native <details> accordion (D-10 motion-safe chevron, 44px summary)"
      contains: "<details"
    - path: "src/assets/illustrations/hero-deal-flow.svg"
      provides: "DESIGN-05 converging-linework hero (aria-hidden, currentColor)"
      contains: "currentColor"
    - path: "src/icons/practice-ip-tech.svg"
      provides: "DESIGN-06 IP & Tech practice icon"
      contains: "currentColor"
    - path: "src/icons/practice-tax.svg"
      provides: "DESIGN-06 Tax practice icon"
      contains: "currentColor"
    - path: "src/pages/_design.astro"
      provides: "gallery rendering all 8 components with real placeholder content"
      contains: "data-component=\"FaqAccordion\""
  key_links:
    - from: "src/pages/_design.astro"
      to: "all 8 section components"
      via: "import + render with FIRM_BRIEF placeholder props"
      pattern: "Hero|AttorneyCard|TestimonialQuote|DealsGrid|FeeStructureBand|CtaBlock|FaqAccordion"
    - from: "src/components/sections/Hero.astro"
      to: "src/assets/illustrations/hero-deal-flow.svg"
      via: "direct inline SVG import (not astro-icon)"
      pattern: "hero-deal-flow"
    - from: "src/components/sections/PracticeAreaCard.astro"
      to: "src/icons/practice-{ip-tech,tax}.svg"
      via: "<Icon name> on /_design"
      pattern: "practice-ip-tech|practice-tax"
---

<objective>
Complete the component library: add the remaining 7 of the 8 locked section components on top of the
02-01 foundation slice, author the converging-linework hero SVG plus the two remaining thin-line practice
icons, and expand `/_design` to render all 8 components with realistic FIRM_BRIEF placeholder content
(the Athelas–Commure $6B testimonial, the 5 attorneys, the 3 practice areas, the fee band, the FAQs).
This is the slice where Jon can see the full design system on a Vercel preview.

Purpose: Deliver DESIGN-04 (8 reusable components), DESIGN-05 (hero SVG), and the rest of DESIGN-06 (3 icons).
Output: Hero, AttorneyCard, TestimonialQuote, DealsGrid, FeeStructureBand, CtaBlock, FaqAccordion;
hero-deal-flow.svg + 2 practice icons; placeholder headshots under src/assets/; full /_design gallery;
gallery + a11y-interactions + assets-budget tests un-skipped & green.

All components use the namespace-mapped Tailwind v4 utilities (per the 02-01 decision). FaqAccordion is
native `<details>` — ZERO client JS (a `client:*` directive is a regression). Never hardcode hex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/FIRM_BRIEF.md
@.planning/phases/02-design-system-visual-identity/02-UI-SPEC.md
@.planning/phases/02-design-system-visual-identity/02-PATTERNS.md
@.planning/phases/02-design-system-visual-identity/02-RESEARCH.md
@.planning/phases/02-design-system-visual-identity/02-01-SUMMARY.md

<interfaces>
<!-- Foundation shipped by 02-01 — reuse, do not recreate. -->
Tokens live in src/styles/global.css @theme: utilities text-display/-h1/-h2/-h3/-body-lg/-body/-small,
  bg-bg/bg-bg-elevated, text-text/-text-muted/-accent/-accent-fg, border-border, shadow-card/-card-hover,
  rounded-card/-button, p-section/py-section/gap-section, px-gutter. Reduced-motion backstop in global.css.
UI primitives (02-01): src/components/ui/Button.astro (variant primary|secondary; href→<a> else <button>),
  TextLink.astro, Eyebrow.astro. Compose these — do not re-author buttons inline.
src/components/sections/PracticeAreaCard.astro already exists (the card shell analog for AttorneyCard/DealsGrid).
astro-icon usage: `import { Icon } from 'astro-icon/components';` then `<Icon name="practice-tax" class="size-10 text-accent" aria-hidden="true" />`.
Content-collection image() assets must live under src/ (DECISIONS.md) → put placeholder headshots in src/assets/headshots/.
Astro <Image>: `import { Image } from 'astro:assets';` with explicit width/height (no CLS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero (+ hero SVG) + remaining 2 practice icons + CtaBlock + FeeStructureBand</name>
  <files>src/assets/illustrations/hero-deal-flow.svg, src/icons/practice-ip-tech.svg, src/icons/practice-tax.svg, src/components/sections/Hero.astro, src/components/sections/CtaBlock.astro, src/components/sections/FeeStructureBand.astro</files>
  <read_first>
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §"1. Hero" / §"6. FeeStructureBand" / §"7. CtaBlock" / §"Creative Art Contract" / §"Copywriting Contract"
    - .planning/phases/02-design-system-visual-identity/02-PATTERNS.md §Hero.astro / §CtaBlock.astro / §FeeStructureBand.astro / §"Asset Files (SVG)"
    - .planning/phases/02-design-system-visual-identity/02-RESEARCH.md §"Pattern 5 (currentColor SVG)" / §"Pitfall 6 (decorative vs meaningful)"
    - src/components/ui/Button.astro (02-01) ; src/components/sections/PracticeAreaCard.astro (02-01 icon idiom) ; src/pages/index.astro (centered-section analog)
  </read_first>
  <action>
    Author `src/assets/illustrations/hero-deal-flow.svg` — the converging-linework "deal flow" concept: fine thin
    lines from multiple origins converging to a single point/knot (separate parties becoming one closed deal).
    `fill="none" stroke="currentColor"`, thin strokes, editorial/minimal, `aria-hidden="true"`, ≤ 200 KB (expect
    < 15 KB). NOT a network-of-nodes graph (D-12). Author `src/icons/practice-ip-tech.svg` (thin-line protected/keyed
    mark) and `src/icons/practice-tax.svg` (thin-line converging/structured-ledger mark) — same line system as the
    hero and the 02-01 M&A icon: `viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.25"
    stroke-linecap="round" stroke-linejoin="round"`, ≤ 200 KB each, currentColor only.
    Create `src/components/sections/Hero.astro` (data-component="Hero"): Props { headline, subhead?, ctaLabel,
    ctaHref, secondaryLabel?, secondaryHref? }; H1 uses `text-display font-extrabold` (the only --text-display
    usage); inline the hero SVG directly (NOT astro-icon) with `aria-hidden="true"`; compose `Button` for the
    primary (and optional secondary) CTA; responsive two-column ≥md / single-column mobile.
    Create `src/components/sections/CtaBlock.astro` (data-component="CtaBlock"): Props { heading, body?, ctaLabel,
    ctaHref }; centered `max-w-3xl py-section`; composes primary `Button`.
    Create `src/components/sections/FeeStructureBand.astro` (data-component="FeeStructureBand"): Props { heading,
    body, note? }; banded `bg-bg-elevated rounded-card shadow-card` surface, small accent leading marker, inner
    `mx-auto max-w-6xl px-6`; static. NO published rates (LEGAL-08 / scope).
    Use the locked microcopy from UI-SPEC (Hero headline "Team work to get good results.", CTA "Start a
    conversation" → /contact, etc.). No banned Rule 7.4 terms; no outcome guarantees.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - hero-deal-flow.svg + practice-ip-tech.svg + practice-tax.svg exist, currentColor only, each ≤ 200 KB
    - Hero/CtaBlock/FeeStructureBand exist with their data-component markers, token-only, composing the Button primitive
    - Hero SVG is inlined and aria-hidden; H1 carries the real heading text
    - `npm run build` succeeds; no hardcoded hex; no client:* directive
  </acceptance_criteria>
  <done>Hero + creative art + CtaBlock + FeeStructureBand build clean.</done>
</task>

<task type="auto">
  <name>Task 2: AttorneyCard (+ placeholder headshots) + TestimonialQuote + DealsGrid + FaqAccordion</name>
  <files>src/components/sections/AttorneyCard.astro, src/assets/headshots/, src/components/sections/TestimonialQuote.astro, src/components/sections/DealsGrid.astro, src/components/sections/FaqAccordion.astro</files>
  <read_first>
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §"3. AttorneyCard" / §"4. TestimonialQuote" / §"5. DealsGrid" / §"8. FaqAccordion" / §"Motion & Interaction Contract"
    - .planning/phases/02-design-system-visual-identity/02-PATTERNS.md §AttorneyCard.astro / §TestimonialQuote.astro / §DealsGrid.astro / §FaqAccordion.astro
    - .planning/phases/02-design-system-visual-identity/02-RESEARCH.md §"Pattern 4 (FaqAccordion native <details>)" / §"Pitfall 7 (sharp/<Image> assets under src/)"
    - src/components/sections/PracticeAreaCard.astro (02-01 card-shell analog) ; src/components/chrome/SiteHeader.astro (.map() list-render analog) ; src/content.config.ts (image() usage)
    - .planning/FIRM_BRIEF.md (the 5 attorneys, Athelas–Commure testimonial, representative deals)
  </read_first>
  <action>
    Create placeholder headshots under `src/assets/headshots/` (neutral monogram/silhouette SVG or tiny raster
    tiles, ATTY-08 — NO real photos), each ≤ 200 KB. Create `src/components/sections/AttorneyCard.astro`
    (data-component="AttorneyCard"): same floating-card shell as PracticeAreaCard; Astro `<Image>` headshot with
    explicit width/height (no CLS); Props { name, role, focus, photo, href, alt }; descriptive `alt` (A11Y-02);
    motion-safe hover-lift; focus-visible ring. NEVER reference "Fishbien" (ATTY-11).
    Create `src/components/sections/TestimonialQuote.astro` (data-component="TestimonialQuote"): `<blockquote>` +
    `<cite>` semantics; `--text-body-lg`/`--text-h3` weight 500 on bg; small accent open-quote glyph; optional
    `disclosure?` slot (for later LEGAL-06 <Disclaimer>); `max-w-3xl mx-auto`.
    Create `src/components/sections/DealsGrid.astro` (data-component="DealsGrid"): Props { deals: {title;
    amount?; context?}[]; heading? }; `.map()` over deals into floating cells (bg-bg-elevated rounded-card
    shadow-card); responsive grid with gap-step utilities. Only cleared/already-public placeholder deals.
    Create `src/components/sections/FaqAccordion.astro` (data-component="FaqAccordion"): Props { items: {q; a}[];
    heading? }; render native `<details>`/`<summary>` per item (RESEARCH Pattern 4 — ZERO JS); panel
    `bg-bg-elevated rounded-card border border-border`; `<summary>` = question (H3-weight) + accent chevron that
    rotates 180° on open `motion-safe:`; summary padded to ≥44px; `focus-visible:ring-2 focus-visible:ring-accent`.
    NO `client:*` directive.
    Use the exact placeholder content from UI-SPEC §3–5/§8 (Daniel Brian / Commure testimonial; Athelas/Commure
    $6B, Mode Analytics $200M, Illumina vs Roche, representative parties; the 3 partner-led FAQs).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - AttorneyCard/TestimonialQuote/DealsGrid/FaqAccordion exist with their data-component markers, token-only
    - FaqAccordion uses native <details>/<summary> with motion-safe chevron + ≥44px summary; NO client:* directive
    - AttorneyCard uses <Image> with explicit dimensions; placeholder headshots live under src/assets/; no "Fishbien"
    - TestimonialQuote uses <blockquote>/<cite>; DealsGrid renders cleared placeholder deals only
    - `npm run build` succeeds; no hardcoded hex
  </acceptance_criteria>
  <done>Remaining 4 section components build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Expand /_design to all 8 components; un-skip gallery + a11y-interactions + assets-budget tests</name>
  <files>src/pages/_design.astro, tests/gallery.spec.ts, tests/a11y-interactions.spec.ts, tests/assets-budget.spec.ts</files>
  <read_first>
    - src/pages/_design.astro (02-01 — currently renders one PracticeAreaCard)
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §"Component Inventory" (all placeholder content) / §"Hidden Gallery Route"
    - .planning/phases/02-design-system-visual-identity/02-PATTERNS.md §_design.astro / §"Test Files (Wave 0)"
    - tests/gallery.spec.ts, tests/a11y-interactions.spec.ts, tests/assets-budget.spec.ts (skipped scaffolds from 02-00 to un-skip)
  </read_first>
  <action>
    Expand `src/pages/_design.astro` to import and render ALL 8 section components inside labeled sections, each
    with the realistic FIRM_BRIEF placeholder content from UI-SPEC §"Component Inventory": Hero; 3
    PracticeAreaCards (M&A + IP&Tech + Tax, using all 3 icons); the 5 AttorneyCards (Belcher, Smolen, Van Loo,
    Zhang, Jiang — never Fishbien); the Daniel Brian / Commure TestimonialQuote; the DealsGrid; the
    FeeStructureBand; a CtaBlock; the 3-question FaqAccordion. Keep the `noindex` prop. Keep route `/_design`
    (or the renamed route if 02-01 had to rename it — match that).
    Un-skip `tests/gallery.spec.ts` (one [data-component] per the 8 + ≥3 inline icon <svg>),
    `tests/a11y-interactions.spec.ts` (<details> present + motion-safe + focus-visible:ring present), and the
    asset-existence portion of `tests/assets-budget.spec.ts` (hero SVG + 3 practice icons + woff2 + headshots
    exist and are ≤ 200 KB).
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/gallery.spec.ts tests/a11y-interactions.spec.ts tests/assets-budget.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - /_design renders all 8 data-component markers exactly once each and ≥3 inline icon <svg>
    - tests/gallery.spec.ts, tests/a11y-interactions.spec.ts, tests/assets-budget.spec.ts are ACTIVE and pass
    - All assets present and ≤ 200 KB; no "Fishbien" string in the built page
    - `npm run build` green; full Phase 2 suite has no failures
  </acceptance_criteria>
  <done>Full gallery live; gallery/a11y/assets tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → browser (CSP) | Components must add no remote asset and no client script; CSP report-only stays clean. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering (XSS) | hand-authored hero + 2 icons | mitigate | All SVG hand-authored line art (no embedded script); astro-icon runs SVGO on the icons; hero inlined as authored. |
| T-02-04 | Tampering (CSP erosion) | FaqAccordion | mitigate | Native `<details>` — ZERO JS, no `client:*`, no new `script-src` need (RESEARCH Pattern 4). Enforced by tests/a11y-interactions.spec.ts. |
| T-02-02 | Information Disclosure | expanded /_design | mitigate | Inherits 02-01 noindex + sitemap filter; gallery uses only public placeholder content (no uncleared client names beyond FIRM_BRIEF public deals). |
</threat_model>

<verification>
- `npm run build` green.
- `npm run test` (full Phase 2 suite) green: contrast, design-tokens, fonts-selfhost, design-route-hidden, gallery, a11y-interactions, assets-budget.
- `/_design` shows all 8 components with realistic content; FaqAccordion toggles with no JS.
</verification>

<success_criteria>
All 8 locked components exist, are token-only and JS-free, and render with realistic FIRM_BRIEF content on
`/_design`. The hero SVG and 3 practice icons are on disk (currentColor, ≤200KB). Six of the seven Wave-0
tests are now active and green (contrast was green from 02-00). The design system is reviewable on a Vercel preview.
</success_criteria>

<output>
Create `.planning/phases/02-design-system-visual-identity/02-02-SUMMARY.md` when done.
</output>
