---
phase: 02-design-system-visual-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/assets/fonts/
  - src/icons/practice-mergers-acquisitions.svg
  - src/components/ui/Button.astro
  - src/components/ui/TextLink.astro
  - src/components/ui/Eyebrow.astro
  - src/components/sections/PracticeAreaCard.astro
  - src/pages/_design.astro
  - tests/design-tokens.spec.ts
  - tests/fonts-selfhost.spec.ts
  - tests/design-route-hidden.spec.ts
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04, DESIGN-06, DESIGN-07, DESIGN-08]
must_haves:
  truths:
    - "The locked Direction-B palette, fluid type scale, spacing rhythm, radius and shadow tokens live in src/styles/global.css @theme (single source of truth)"
    - "Hanken Grotesk is self-hosted via Astro Fonts API and served from 'self' — no Google Fonts CDN URL anywhere in dist/"
    - "Visiting /_design shows ONE real component (PracticeAreaCard) styled entirely from tokens, with a real practice-area icon, rendering a real practice area"
    - "/_design is noindex even in PROD and is excluded from the sitemap"
  artifacts:
    - path: "src/styles/global.css"
      provides: "DESIGN-02 @theme tokens — D-22 names, D-01 values, + type/spacing/radius/shadow scales + reduced-motion guard"
      contains: "#9A3F1A"
    - path: "astro.config.mjs"
      provides: "fonts: fontProviders.local() Hanken; icon(); sitemap filter excluding /_design"
      contains: "fontProviders.local()"
    - path: "src/layouts/BaseLayout.astro"
      provides: "<Font preload/> wiring + noindex? prop folding into one robots meta"
      contains: "noindex"
    - path: "src/components/ui/Button.astro"
      provides: "primary/secondary button primitive (token-only, focus-visible ring, 44px hit area)"
    - path: "src/components/sections/PracticeAreaCard.astro"
      provides: "first vertical-slice section component (data-component marker)"
      contains: "data-component=\"PracticeAreaCard\""
    - path: "src/icons/practice-mergers-acquisitions.svg"
      provides: "DESIGN-06 first thin-line practice icon (currentColor)"
      contains: "currentColor"
    - path: "src/pages/_design.astro"
      provides: "hidden gallery route rendering the first slice"
      contains: "noindex"
  key_links:
    - from: "src/styles/global.css"
      to: "Tailwind v4 utilities"
      via: "@theme namespace auto-generation"
      pattern: "--color-accent|--text-h3|--shadow-card"
    - from: "src/layouts/BaseLayout.astro"
      to: "self-hosted Hanken woff2"
      via: "<Font cssVariable=\"--font-hanken\" preload />"
      pattern: "Font.*--font-hanken"
    - from: "src/pages/_design.astro"
      to: "src/components/sections/PracticeAreaCard.astro"
      via: "import + render with FIRM_BRIEF placeholder content"
      pattern: "PracticeAreaCard"
    - from: "astro.config.mjs"
      to: "sitemap exclusion"
      via: "filter callback"
      pattern: "filter:.*_design"
---

<objective>
Ship the thinnest end-to-end design-system slice: tokens → font → ONE component → rendered on the
gallery → build + Wave-0 tests passing. After this plan, the entire pipeline is proven:
the locked Direction-B palette / fluid type / spacing / shadow tokens live in `global.css` `@theme`,
Hanken Grotesk is self-hosted CSP-clean, the UI primitives (Button, TextLink, Eyebrow) and the first
section component (`PracticeAreaCard`) render — token-styled, with a real thin-line practice icon — on a
hidden, noindex, sitemap-excluded `/_design` page. A future restyle is already a single-file edit.

Purpose: De-risk every hard platform integration (Tailwind v4 @theme, Astro Fonts API, astro-icon,
sitemap filter, noindex prop) in one slice, then 02-02/02-03 add the remaining components and creative art.
Output: rewritten global.css, fonts+icon+sitemap config, font assets, 3 ui primitives, PracticeAreaCard,
the first practice icon, /_design, and three Wave-0 tests un-skipped & green.

Token-syntax decision (state for the verifier): NEW Phase 2 components use the Tailwind v4
**namespace-mapped utilities** (`bg-bg-elevated`, `text-accent`, `text-h3`, `shadow-card`, `p-section`,
`border-border`) — NOT the Phase 1 arbitrary-value form `text-[color:var(--color-text)]`. This is the
approved standardization (UI-SPEC §"Token-name contract", PATTERNS §"Critical Cross-Cutting Convention").
Phase 1 chrome is retrofitted in 02-03. The new syntax is correct, not a deviation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-visual-identity/02-CONTEXT.md
@.planning/phases/02-design-system-visual-identity/02-RESEARCH.md
@.planning/phases/02-design-system-visual-identity/02-UI-SPEC.md
@.planning/phases/02-design-system-visual-identity/02-PATTERNS.md

<interfaces>
<!-- Exact source-of-truth the executor needs; do not explore the codebase to rediscover these. -->
Current src/styles/global.css @theme block to REWRITE (keep names, swap values, ADD scales):
  --color-text → #111111 ; --color-text-muted → #52524E ; --color-bg → #F8F5F0 ;
  --color-bg-elevated → #FDFCFA ; --color-border → #D9D2C5 ; --color-accent → #9A3F1A ; --color-accent-fg → #F8F5F0
  --font-sans → prepend var(--font-hanken) to the existing fallback stack
Keep the existing @layer base block (border-color reset + body bg/text/font) intact — values flow through.
ADD type tokens (UI-SPEC §Typography, exact clamp stops): --text-display, --text-h1, --text-h2, --text-h3,
  --text-body-lg, --text-body, --text-small. ADD --spacing-section, --spacing-gutter, --radius-card,
  --radius-button, --shadow-card, --shadow-card-hover (UI-SPEC §Spacing / §Radius&Shadow exact values).
Current astro.config.mjs: integrations:[mdx(), sitemap()]; vite plugins:[tailwindcss()]; site 'https://bsvlaw.com'.
BaseLayout.astro Props: { title, description, canonical?, ogImage? } ; robots from `const isPreview = !import.meta.env.PROD`.
PracticeAreaCard skeleton (RESEARCH Code Examples — usable verbatim):
```astro
---
import { Icon } from 'astro-icon/components';
interface Props { title: string; blurb: string; icon: string; href: string; }
const { title, blurb, icon, href } = Astro.props;
---
<a href={href} data-component="PracticeAreaCard"
   class="group block rounded-card bg-bg-elevated p-8 shadow-card border border-border
          transition-transform duration-200 motion-safe:hover:-translate-y-1 motion-safe:hover:shadow-card-hover">
  <Icon name={icon} class="size-10 text-accent" aria-hidden="true" />
  <h3 class="mt-4 text-h3 font-bold text-text">{title}</h3>
  <p class="mt-2 text-body text-text-muted">{blurb}</p>
</a>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install astro-icon + sharp; acquire Hanken Grotesk woff2; wire fonts + icon + sitemap filter</name>
  <files>package.json, astro.config.mjs, src/assets/fonts/, src/layouts/BaseLayout.astro</files>
  <read_first>
    - .planning/phases/02-design-system-visual-identity/02-RESEARCH.md §"Standard Stack" / §"Pattern 2 (Fonts API)" / §"Pattern 3 (astro-icon)" / §"Sitemap exclusion" / §"Package Legitimacy Audit"
    - .planning/phases/02-design-system-visual-identity/02-PATTERNS.md §astro.config.mjs / §package.json / §BaseLayout.astro
    - astro.config.mjs, package.json, src/layouts/BaseLayout.astro (current state)
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §Typography (weights 400/500/700/800)
  </read_first>
  <action>
    Install `astro-icon@^1.1.5` and pin `sharp@^0.34.5` as dependencies (caret style, alphabetical) per the
    Package Legitimacy Audit (astro-icon: official Astro-team author natemoo-re, ~4.4yr registry age, no
    postinstall — provenance accepted per DECISIONS.md gitleaks-binary precedent; no human checkpoint).
    Acquire the genuine OFL-licensed Hanken Grotesk woff2 files for weights 400/500/700/800 WITHOUT adding any
    Google Fonts CDN link: download from the Fontsource package/CDN (e.g. the @fontsource files on jsDelivr) or
    npm-extract, and COMMIT them under `src/assets/fonts/` (e.g. HankenGrotesk-Regular/Medium/Bold/ExtraBold.woff2).
    Verify each file is real woff2 (starts with `wOF2`) and ≤ 204800 bytes. If a single variable woff2 ≤ ~100KB is
    obtainable that covers 400-800, prefer that and adjust the variants config accordingly. If — and only if —
    the woff2 cannot be obtained programmatically, set this plan's autonomous to false and emit a
    checkpoint:human-action requesting the woff2; otherwise proceed autonomously.
    In `astro.config.mjs`: change import to `import { defineConfig, fontProviders } from 'astro/config';`, add
    `import icon from 'astro-icon';`; add a top-level `fonts: [{ provider: fontProviders.local(), name:
    'Hanken Grotesk', cssVariable: '--font-hanken', fallbacks: ['sans-serif'], options: { variants: [...the
    committed weights with src paths...] } }]`; change integrations to
    `[mdx(), sitemap({ filter: (page) => page !== 'https://bsvlaw.com/_design' }), icon()]`.
    In `BaseLayout.astro`: add `import { Font } from 'astro:assets';` and add `<Font cssVariable="--font-hanken" preload />`
    in `<head>`. Add `noindex?: boolean` to Props (default false), destructure it, and replace the robots logic
    with one meta: `const robots = (isPreview || noindex) ? 'noindex, nofollow' : 'index, follow';` →
    `<meta name="robots" content={robots} />`. Keep SeoHead/JsonLd/SkipToContent/header/footer wiring untouched.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists astro-icon ^1.1.5 and sharp ^0.34.5
    - src/assets/fonts/ contains real woff2 files (wOF2 magic bytes), each ≤ 204800 bytes
    - astro.config.mjs has the fonts[] block (fontProviders.local), icon() integration, and the sitemap filter excluding /_design
    - BaseLayout renders <Font ... preload/> and emits exactly one <meta name="robots"> driven by (isPreview || noindex)
    - `npm run build` succeeds; no `fonts.googleapis`/`fonts.gstatic` string appears in dist/
  </acceptance_criteria>
  <done>Build green; fonts self-hosted; icon + sitemap filter configured; BaseLayout has the noindex prop + Font preload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite global.css @theme (palette + scales + reduced-motion guard); add design-tokens + fonts-selfhost tests</name>
  <files>src/styles/global.css, tests/design-tokens.spec.ts, tests/fonts-selfhost.spec.ts</files>
  <read_first>
    - src/styles/global.css (current placeholder @theme + @layer base — keep structure)
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §Color Tokens / §Typography / §Spacing / §"Radius & Shadow tokens"
    - .planning/phases/02-design-system-visual-identity/02-PATTERNS.md §global.css (exact rewrite + reduced-motion guard)
    - .planning/phases/02-design-system-visual-identity/02-RESEARCH.md §"Pattern 1" (namespace→utility mapping)
    - tests/design-tokens.spec.ts, tests/fonts-selfhost.spec.ts (the skipped scaffolds from 02-00 to un-skip)
  </read_first>
  <behavior>
    - design-tokens.spec: after build, dist/_astro/*.css contains `#9a3f1a` (accent) AND `#f8f5f0` (bg)
    - fonts-selfhost.spec: dist/ contains ZERO `fonts.googleapis`/`fonts.gstatic`; a compiled CSS file contains `font-display` and `@font-face`
  </behavior>
  <action>
    Rewrite the `@theme {}` block in `src/styles/global.css`: keep the D-22 token NAMES, set the D-01 hex
    VALUES (--color-text #111111, --color-text-muted #52524E, --color-bg #F8F5F0, --color-bg-elevated #FDFCFA,
    --color-border #D9D2C5, --color-accent #9A3F1A, --color-accent-fg #F8F5F0); prepend `var(--font-hanken)`
    to --font-sans. ADD the fluid type tokens with the EXACT clamp stops + the static body sizes from UI-SPEC
    §Typography (--text-display, --text-h1, --text-h2, --text-h3, --text-body-lg, --text-body, --text-small);
    ADD --spacing-section `clamp(4rem, 2.5rem + 7vw, 8rem)`, --spacing-gutter `clamp(1.25rem, 0.75rem + 2vw, 2rem)`;
    ADD --radius-card 0.75rem, --radius-button 0.5rem; ADD --shadow-card and --shadow-card-hover with the exact
    rust-tinted rgba(60,30,15,...) recipes from UI-SPEC §"Radius & Shadow tokens". Keep the existing `@layer base`
    block intact. APPEND the reduced-motion backstop `@media (prefers-reduced-motion: reduce){ *,*::before,*::after{
    animation:none!important;transition:none!important;scroll-behavior:auto!important } }`.
    Un-skip `tests/design-tokens.spec.ts` and `tests/fonts-selfhost.spec.ts` (remove the describe.skip / UNSKIP
    comment) so both run against the build.
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/design-tokens.spec.ts tests/fonts-selfhost.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - global.css @theme has all D-01 hex values, all 7 --text-* tokens, both --spacing-*, both --radius-*, both --shadow-* tokens
    - The @layer base block and the reduced-motion guard are both present
    - tests/design-tokens.spec.ts and tests/fonts-selfhost.spec.ts are ACTIVE (no skip) and pass
    - No hardcoded `#` hex other than inside the @theme block and the shadow recipes
  </acceptance_criteria>
  <done>Tokens compiled into dist CSS; both token/font tests green.</done>
</task>

<task type="auto">
  <name>Task 3: UI primitives + PracticeAreaCard + first practice icon + /_design slice; un-skip design-route-hidden test</name>
  <files>src/components/ui/Button.astro, src/components/ui/TextLink.astro, src/components/ui/Eyebrow.astro, src/components/sections/PracticeAreaCard.astro, src/icons/practice-mergers-acquisitions.svg, src/pages/_design.astro, tests/design-route-hidden.spec.ts</files>
  <read_first>
    - .planning/phases/02-design-system-visual-identity/02-UI-SPEC.md §"UI primitives" / §"2. PracticeAreaCard" / §"Creative Art Contract" / §"Hidden Gallery Route" / §"Motion & Interaction Contract"
    - .planning/phases/02-design-system-visual-identity/02-PATTERNS.md §Button.astro / §TextLink.astro / §Eyebrow.astro / §PracticeAreaCard.astro / §_design.astro / §"Asset Files (SVG)"
    - src/components/chrome/SiteHeader.astro (Props + class:list idiom analog) ; src/components/legal/Disclaimer.astro (typed Props wrapper analog) ; src/pages/index.astro (page-wrapping-BaseLayout analog)
    - tests/design-route-hidden.spec.ts (skipped scaffold from 02-00 to un-skip)
  </read_first>
  <action>
    Create `src/components/ui/Button.astro` (variants primary `bg-accent text-accent-fg` hover-deepen via
    `transition-colors motion-safe:hover:bg-accent/90`, secondary `border border-text text-text bg-transparent`;
    polymorphic `<a href>` else `<button>`; `rounded-button`; ≥44px hit area via padding;
    `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2
    focus-visible:ring-offset-bg`). Create `src/components/ui/TextLink.astro` (`text-accent`, always-visible base
    underline + `motion-safe:` width-grow `after:` pseudo, focus-visible ring). Create `src/components/ui/Eyebrow.astro`
    (`text-small uppercase text-text-muted tracking-wide` wrapper + slot). All token-only, NO hardcoded hex.
    Author `src/icons/practice-mergers-acquisitions.svg` — thin-line abstract of two forms merging into one,
    `viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.25" stroke-linecap="round"
    stroke-linejoin="round"`, decorative, ≤ 200 KB (expect < 4 KB).
    Create `src/components/sections/PracticeAreaCard.astro` per the RESEARCH skeleton in <interfaces>
    (data-component="PracticeAreaCard", namespace utilities, motion-safe hover-lift).
    Create `src/pages/_design.astro` wrapping BaseLayout with `noindex` (the new prop — do NOT add a slotted
    `<meta robots>`), title "Design System (internal)". Render ONE PracticeAreaCard with the real M&A placeholder
    content from UI-SPEC §2 (icon `practice-mergers-acquisitions`, title "Mergers & Acquisitions", the locked
    blurb, href `/practice-areas/mergers-acquisitions`). Confirm at build that the emitted route is `/_design`
    (RESEARCH A2) — if Astro drops the underscore page, rename to `/design-system` and update the sitemap filter
    + tests, recording the change in the summary.
    Un-skip `tests/design-route-hidden.spec.ts` (noindex present + `_design` absent from sitemap).
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/design-route-hidden.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Button/TextLink/Eyebrow exist, token-only, with focus-visible accent rings and motion-safe transitions
    - src/icons/practice-mergers-acquisitions.svg uses currentColor, no hardcoded hex, ≤ 200 KB
    - PracticeAreaCard carries data-component="PracticeAreaCard" and renders the M&A icon via <Icon>
    - /_design builds, renders one PracticeAreaCard, is noindex, and is absent from dist sitemap
    - tests/design-route-hidden.spec.ts is ACTIVE and passes; `npm run build` green
    - No `client:*` directive anywhere; no hardcoded hex in any component
  </acceptance_criteria>
  <done>First end-to-end slice live on /_design; route hidden test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → browser (CSP) | All Phase 2 assets must stay same-origin so the report-only CSP (`font-src 'self'`, `script-src 'self'`) stays clean. |
| npm registry → repo | astro-icon + sharp installs cross the supply-chain boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | webfont source | mitigate | Self-host Hanken via Astro Fonts API `local()`; commit woff2 to src/; ZERO Google Fonts CDN link → font-src 'self' clean (D-07). Enforced by tests/fonts-selfhost.spec.ts. |
| T-02-02 | Information Disclosure | /_design route | mitigate | noindex (BaseLayout prop, PROD too) + sitemap filter; no confidential content (D-16/D-20 precedent). Enforced by tests/design-route-hidden.spec.ts. |
| T-02-03 | Tampering (XSS) | hand-authored SVG | mitigate | SVG is hand-authored line art (no embedded script); astro-icon runs SVGO which strips scripts. |
| T-02-SC | Tampering | astro-icon/sharp install | accept | astro-icon: official Astro-team author (natemoo-re), ~4.4yr registry age, no postinstall script, named in DESIGN-06/STACK.md; sharp: ubiquitous, Astro's default image engine. Provenance verified in RESEARCH Package Legitimacy Audit; accepted per DECISIONS.md supply-chain precedent — no blocking human checkpoint. |
</threat_model>

<verification>
- `npm run build` succeeds.
- `npx playwright test tests/contrast.spec.ts tests/design-tokens.spec.ts tests/fonts-selfhost.spec.ts tests/design-route-hidden.spec.ts` all green.
- `dist/` contains the compiled accent/bg hexes and a self-hosted `@font-face`; ZERO Google Fonts URLs.
- `/_design` is noindex and not in `dist/client/sitemap-0.xml`.
</verification>

<success_criteria>
The design-system pipeline is proven end to end: tokens (palette + type + spacing + shadow) in one CSS file,
Hanken self-hosted CSP-clean, three UI primitives + PracticeAreaCard + a real practice icon rendering on a
hidden noindex/sitemap-excluded `/_design`. Build green; 4 Wave-0 tests green. A restyle is a single-file edit.
</success_criteria>

<output>
Create `.planning/phases/02-design-system-visual-identity/02-01-SUMMARY.md` when done.
</output>
