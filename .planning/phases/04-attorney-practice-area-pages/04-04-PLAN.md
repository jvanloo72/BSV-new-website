---
phase: 04-attorney-practice-area-pages
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - src/content/practiceAreas/mergers-acquisitions.mdx
  - src/content/practiceAreas/intellectual-property-technology-transactions.mdx
  - src/content/practiceAreas/tax.mdx
autonomous: false
requirements: [PRAC-08, SEO-05, LEGAL-10]
must_haves:
  truths:
    - "Jon reviews and approves the drafted FAQs BEFORE any FAQ copy is written into MDX or committed (D-13 hard gate)"
    - "Each practice page renders 3-5 approved FAQs in a FaqAccordion and carries valid FAQPage JSON-LD whose mainEntity Q/A count matches the visible FAQs"
    - "FAQ answers are accurate and non-promissory — no guarantees, no results predictions (Rule 7.1 / LEGAL-10)"
  artifacts:
    - path: "src/content/practiceAreas/mergers-acquisitions.mdx"
      provides: "approved M&A FAQs in the faqs array"
      contains: "faqs:"
    - path: "src/content/practiceAreas/tax.mdx"
      provides: "approved Tax FAQs in the faqs array"
      contains: "faqs:"
  key_links:
    - from: "src/content/practiceAreas/*.mdx faqs"
      to: "PracticeAreaLayout buildFaqPageLd"
      via: "populated faqs array triggers the guarded FAQPage emission"
      pattern: "faqs:"
---

<objective>
Close the D-13 human approval gate: present the drafted FAQs (staged in
FAQ-DRAFT.md by Plan 03) to Jon, and ONLY after he approves, write the approved
FAQs into the three practice MDX `faqs` arrays. This populates the previously
guarded FaqAccordion render and FAQPage JSON-LD (PRAC-08 / SEO-05).

Purpose: Encode the locked decision that FAQs must never publish unreviewed
(D-13). This plan is deliberately NOT autonomous — the build pauses for Jon.
Output: Three practice MDX with approved `faqs` populated; FAQPage JSON-LD live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attorney-practice-area-pages/04-CONTEXT.md
@.planning/phases/04-attorney-practice-area-pages/FAQ-DRAFT.md
@.planning/phases/04-attorney-practice-area-pages/04-03-SUMMARY.md
@src/content/practiceAreas/mergers-acquisitions.mdx
@src/content/practiceAreas/intellectual-property-technology-transactions.mdx
@src/content/practiceAreas/tax.mdx

<interfaces>
<!-- The faqs schema field this plan populates -->
From src/content.config.ts: practiceAreas.faqs: `z.array(z.object({ question: z.string(), answer: z.string() })).default([])`.
PracticeAreaLayout (Plan 03) already renders FaqAccordion + buildFaqPageLd guarded on `faqs.length > 0` — populating these arrays is all that is needed to light up both.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: FAQ approval gate (D-13) — Jon reviews drafted FAQs before commit</name>
  <what-built>
    Plan 03 drafted 3-5 plain-English, non-promissory FAQs per practice area
    (M&A, IP & Technology, Tax) into
    .planning/phases/04-attorney-practice-area-pages/FAQ-DRAFT.md. No FAQ copy has
    been written into any MDX or committed yet — the practice pages currently render
    with empty faqs arrays.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/04-attorney-practice-area-pages/FAQ-DRAFT.md.
    2. For EACH practice area, read the 3-5 question/answer pairs.
    3. Confirm every answer is accurate, general (not advice for a specific matter),
       and non-promissory: NO outcome guarantees, NO "results" claims, NO "expert"/
       "specialist"/"specialize" (Rule 7.1 / 7.4 / LEGAL-10).
    4. Confirm the "how much does it cost" answer matches the fee-band copy (hourly +
       estimate, no published rate).
    5. Edit any wording directly in FAQ-DRAFT.md, OR reply with the exact changes.
  </how-to-verify>
  <resume-signal>Reply "approved" (optionally with edits applied to FAQ-DRAFT.md), or provide corrected FAQ text. The executor will not write any FAQ into MDX until you approve.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the approved FAQs into the three practice MDX faqs arrays</name>
  <read_first>
    - .planning/phases/04-attorney-practice-area-pages/FAQ-DRAFT.md (the APPROVED version after Task 1)
    - src/content/practiceAreas/mergers-acquisitions.mdx (faqs: [] to populate)
    - src/content/practiceAreas/intellectual-property-technology-transactions.mdx
    - src/content/practiceAreas/tax.mdx
    - src/content.config.ts (practiceAreas.faqs shape: {question, answer}[])
  </read_first>
  <action>
    ONLY after Task 1 approval: transcribe the approved FAQs from FAQ-DRAFT.md into
    each practice MDX `faqs` array as `{ question, answer }` objects (3-5 per area),
    matching the schema field names exactly. Apply any edits Jon made during the gate.
    Do not alter any other frontmatter or the bodies. Populating these arrays
    automatically lights up the FaqAccordion render and the guarded FAQPage JSON-LD
    that PracticeAreaLayout already wired in Plan 03 (PRAC-08 / SEO-05).
  </action>
  <verify>
    <automated>npm run lint:legal && npm run build && npx playwright test tests/faqpage-jsonld.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `npm run lint:legal` exits 0 (approved FAQ copy contains no banned term).
    - behavior: tests/faqpage-jsonld.spec.ts passes — each practice page has valid FAQPage JSON-LD and the mainEntity Question/Answer count equals the visible FaqAccordion <details> count.
    - source: each of the three practice MDX has a non-empty faqs array (3-5 entries).
  </acceptance_criteria>
  <done>Approved FAQs written into all three practice MDX; FaqAccordion + FAQPage JSON-LD render with matching Q/A counts; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafted FAQ → published FAQ | Human approval gate is the boundary; unreviewed copy must not cross it |
| FAQ strings → FAQPage JSON-LD | Approved answers serialized into ld+json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Compliance (Rule 7.1 / LEGAL-10) | FAQ answers | mitigate | Blocking human-verify gate (D-13) — Jon, the attorney of record, approves accuracy + non-promissory wording before any commit |
| T-04-16 | Compliance (Rule 7.4) | FAQ copy | mitigate | npm run lint:legal runs on the populated MDX before build |
| T-04-17 | Tampering / XSS | FAQ strings in FAQPage JSON-LD | accept | JsonLd.astro JSON.stringify escaping |
</threat_model>

<verification>
- Task 1 is a blocking checkpoint — no FAQ reaches MDX before "approved".
- `npm run lint:legal` clean on populated FAQs.
- `npm run build` + tests/faqpage-jsonld.spec.ts green (Q/A counts match).
</verification>

<success_criteria>
- Jon approved the FAQs before they were committed (D-13 honored).
- Each practice page renders 3-5 approved FAQs + valid FAQPage JSON-LD.
- FAQ answers are non-promissory; lint clean.
</success_criteria>

<output>
Create `.planning/phases/04-attorney-practice-area-pages/04-04-SUMMARY.md` when done
</output>
