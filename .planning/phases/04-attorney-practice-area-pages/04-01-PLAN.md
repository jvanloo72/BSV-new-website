---
phase: 04-attorney-practice-area-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/jsonld.ts
  - scripts/lint-legal.mjs
  - scripts/lint-legal.allowlist.json
  - package.json
  - .planning/CLIENT_DISCLOSURE_CLEARANCE.md
  - tests/person-jsonld.spec.ts
  - tests/faqpage-jsonld.spec.ts
  - tests/lint-legal.spec.ts
  - tests/fishbien-absent.spec.ts
  - tests/draft-exclusion.spec.ts
  - tests/clearance.spec.ts
  - tests/pages-exist.spec.ts
  - tests/lead-attorney-link.spec.ts
  - tests/_fixtures/lint-legal-violation.mdx
autonomous: true
requirements: [ATTY-09, SEO-03, PRAC-08, SEO-05, LEGAL-03, LEGAL-04, ATTY-11, ATTY-06]
must_haves:
  truths:
    - "buildPersonLd(attorney) returns valid Person JSON-LD (no longer throws)"
    - "buildFaqPageLd(faqs) returns valid FAQPage JSON-LD and is safe on empty input"
    - "npm run lint:legal exits non-zero on a banned Rule 7.4 term and zero on clean content"
    - "Aaron Belcher's complete representative-transactions list is cleared in CLIENT_DISCLOSURE_CLEARANCE.md before any deal publishes"
    - "Wave 0 test scaffolds exist so every Wave 2/3 task has an automated verify"
  artifacts:
    - path: "src/lib/jsonld.ts"
      provides: "buildPersonLd implementation + buildFaqPageLd new builder"
      contains: "buildFaqPageLd"
    - path: "scripts/lint-legal.mjs"
      provides: "Rule 7.4 banned-term scanner over src/content/**/*.mdx"
      min_lines: 30
    - path: "scripts/lint-legal.allowlist.json"
      provides: "Documented allowlist of reviewed phrases"
    - path: ".planning/CLIENT_DISCLOSURE_CLEARANCE.md"
      provides: "Aaron 64-deal clearance row (D-15 URL basis)"
      contains: "aaron-belcher-partner"
  key_links:
    - from: "package.json"
      to: "scripts/lint-legal.mjs"
      via: "lint:legal script"
      pattern: "node scripts/lint-legal.mjs"
    - from: "src/lib/jsonld.ts"
      to: "schema-dts"
      via: "WithContext<Person> / WithContext<FAQPage> typing"
      pattern: "WithContext<(Person|FAQPage)>"
---

<objective>
Build the shared foundation that the attorney-pages slice (Plan 02) and
practice-area slices (Plans 03/04) render against: the two JSON-LD builders, the
real `lint:legal` Rule 7.4 scanner wired into the build, the cleared-deal
register entry for Aaron's full list, and the Wave 0 test scaffolds that give
every downstream task an automated verify.

Purpose: Interface-first. Downstream layouts call `buildPersonLd` /
`buildFaqPageLd`; downstream MDX must pass `lint:legal`; downstream deals must be
in the clearance register first. None of those can ship until this plan does.
Output: Implemented `src/lib/jsonld.ts`, `scripts/lint-legal.mjs` + allowlist,
updated `package.json`, populated clearance register, and 8 test files (mostly
scaffolds the later plans complete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-attorney-practice-area-pages/04-CONTEXT.md
@.planning/phases/04-attorney-practice-area-pages/04-RESEARCH.md
@.planning/phases/04-attorney-practice-area-pages/04-PATTERNS.md
@.planning/CLIENT_DISCLOSURE_CLEARANCE.md

<interfaces>
<!-- Contracts extracted from the codebase. Use directly — no exploration needed. -->

From src/lib/jsonld.ts (the working analog buildLegalServiceLd + the two stubs to fill):
- `import type { Article, LegalService, Person, WithContext } from 'schema-dts';`
- `import { SITE } from './site';`  → SITE.name, SITE.baseUrl, SITE.email
- buildLegalServiceLd() shows the pattern: `'@context': 'https://schema.org'`, `'@type'` literal, `as const` on every nested `@type`.
- STUB to replace (lines 52-54): `export function buildPersonLd(_attorney: unknown): WithContext<Person> { throw new Error(...); }`
- buildArticleLd stub is Phase 5 — DO NOT touch it.

From src/content.config.ts (attorneys schema — the buildPersonLd input shape):
- name:string, slug:string, title:enum('Partner'|'Associate'|'Counsel'), barAdmissions:string[].min(1),
  education:{degree,school,year?,honors?}[], focus:string, email:string(email), representativeDeals:{parties,value?,role?,cleared}[]
- practiceAreas.faqs: `{ question: string; answer: string }[]` — the buildFaqPageLd input shape.

From package.json (script to replace, line 28):
- `"lint:legal": "echo 'lint:legal — Phase 1 placeholder; real rules land in Phase 4'"`
- Existing test:* script naming convention to mirror (e.g. "test:jsonld": "playwright test tests/jsonld-legalservice.spec.ts").

From tests/jsonld-legalservice.spec.ts (test convention to mirror):
- `test.beforeAll(() => { execSync('npm run build', { stdio: 'pipe' }); });`
- read `dist/client/<path>/index.html`, `cheerio.load(html)`, iterate `script[type="application/ld+json"]`, JSON.parse, match on `@type`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement buildPersonLd + add buildFaqPageLd in src/lib/jsonld.ts</name>
  <read_first>
    - src/lib/jsonld.ts (the file being modified; buildLegalServiceLd is the verbatim pattern, stubs at lines 52-61)
    - src/lib/site.ts (SITE constants: name, baseUrl, email)
    - src/content.config.ts (attorneys + practiceAreas schemas — input shapes)
    - .planning/phases/04-attorney-practice-area-pages/04-PATTERNS.md (jsonld.ts section, lines 204-241 — exact builder bodies)
    - .planning/phases/04-attorney-practice-area-pages/04-RESEARCH.md (Code Examples lines 359-408)
  </read_first>
  <behavior>
    - buildPersonLd(attorney): returns WithContext<Person> with @context 'https://schema.org', @type 'Person', name=d.name, jobTitle=d.title, url=`${SITE.baseUrl}/attorneys/${d.slug}`, worksFor Organization (SITE.name/SITE.baseUrl), alumniOf mapped from d.education (each {@type:'EducationalOrganization', name: e.school}), knowsAbout from d.focus split on [;,] trimmed non-empty, email=d.email.
    - buildPersonLd MUST NOT emit a sameAs property (D-02 no fabrication — no invented profile URLs).
    - buildFaqPageLd([]) returns an object with no Question entries (empty mainEntity array) — does NOT throw; layout guards emission separately.
    - buildFaqPageLd(faqs) maps each {question,answer} to {@type:'Question', name:question, acceptedAnswer:{@type:'Answer', text:answer}}.
  </behavior>
  <action>
    Replace the throwing buildPersonLd stub with a real implementation typed
    `attorney: CollectionEntry<'attorneys'>` (add `import type { CollectionEntry } from 'astro:content';`).
    Mirror buildLegalServiceLd exactly for the @context/@type header and `as const`
    on every nested @type. Map fields per the behavior block (ATTY-09 / SEO-03).
    Do NOT include sameAs (D-02). Then add a new exported `buildFaqPageLd(faqs: { question: string; answer: string }[]): WithContext<FAQPage>`
    (PRAC-08 / SEO-05); add `FAQPage` to the existing schema-dts import on line 10.
    Leave buildArticleLd (Phase 5) untouched. Token/style rules do not apply (pure TS).
  </action>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `npx astro check` passes with zero type errors after the edit.
    - source: grep `buildFaqPageLd` returns a match in src/lib/jsonld.ts; grep `throw new Error` no longer matches buildPersonLd's body (only buildArticleLd's).
    - source: no `sameAs` token appears in src/lib/jsonld.ts.
  </acceptance_criteria>
  <done>Both builders compile under astro check; buildPersonLd no longer throws; buildFaqPageLd exists and handles empty input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scripts/lint-legal.mjs + allowlist, wire into package.json</name>
  <read_first>
    - package.json (line 28 lint:legal placeholder + test:* naming convention)
    - .planning/phases/04-attorney-practice-area-pages/04-RESEARCH.md (Don't Hand-Roll lines 313-320; Code Examples lines 410-422; Assumption A1 on the word set)
    - .planning/phases/04-attorney-practice-area-pages/04-PATTERNS.md (lint-legal section lines 245-258)
    - .planning/FIRM_BRIEF.md (lines 154, 173 — "Deep tech M&A expertise" / "team of elite specialists" live in .planning, NOT scanned; guard they never migrate into src/content)
  </read_first>
  <behavior>
    - Running the scanner over content containing a non-allowlisted banned root (e.g. "specialist", "specialize", "expert") exits with code 1 and prints file:line:term.
    - Running over clean content (no banned roots, or only allowlisted phrase+file pairs) exits with code 0.
    - "registered to practice before the U.S. Patent and Trademark Office (USPTO)" does NOT match any banned root (Stuart's safe phrasing, D-16).
    - The seeded fixture tests/_fixtures/lint-legal-violation.mdx (containing "expert") triggers exit 1 when scanned; it is excluded from the production glob so it never fails a real build (scan src/content/**/*.mdx only).
  </behavior>
  <action>
    Create scripts/lint-legal.mjs using Node 22 stdlib only (no new deps).
    Scan src/content/**/*.mdx (use node:fs/promises glob; fall back to a recursive
    readdir helper if glob import is unstable — Assumption A4). Match case-insensitive,
    word-boundary banned roots with regex `/\b(special(?:ist|ists|ize[sd]?|izing|ization)|experts?|expertise)\b/gi`.
    Load scripts/lint-legal.allowlist.json (`[{ phrase, file, reason }]`) and suppress a
    hit only when the matched phrase + the file path both match an allowlist row.
    Print `file:line:term` for each violation; `process.exit(violations.length ? 1 : 0)`.
    Seed the allowlist with a documented note that "expertise" is flagged-pending-Jon
    (A1) — leave it banned by default and record the open compliance question as a comment.
    Create scripts/lint-legal.allowlist.json starting as `[]` (no pre-approved phrases).
    Replace package.json line 28 with `"lint:legal": "node scripts/lint-legal.mjs"` and
    add a `"prebuild": "npm run lint:legal"` script so the banned-term gate runs before
    every build (success criterion 4 — runs in CI and fails the build). Mirror existing
    test:* naming when adding any test scripts. Do NOT scan .planning/ (the brief's
    "specialists"/"expertise" wording lives there and must stay out of scope).
  </action>
  <verify>
    <automated>node scripts/lint-legal.mjs; echo "clean exit=$?"; node scripts/lint-legal.mjs tests/_fixtures/lint-legal-violation.mdx 2>&1 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `node scripts/lint-legal.mjs` over current src/content exits 0 (placeholder MDX is clean).
    - behavior: scanning the seeded fixture with "expert" reports a violation and the script's exit path returns non-zero on a banned hit (covered by tests/lint-legal.spec.ts).
    - source: package.json contains `"lint:legal": "node scripts/lint-legal.mjs"` and a `prebuild` that calls lint:legal.
    - test-command: `npx playwright test tests/lint-legal.spec.ts` (scaffolded Task 4) passes once content lands.
  </acceptance_criteria>
  <done>lint:legal is a real Node scanner; exits non-zero on a non-allowlisted banned term; wired as prebuild + CI gate; allowlist file present and empty.</done>
</task>

<task type="auto">
  <name>Task 3: Clear Aaron's 64-deal list (D-15) + write Wave 0 test scaffolds</name>
  <read_first>
    - .planning/CLIENT_DISCLOSURE_CLEARANCE.md (the register being edited; existing rows + Process section)
    - .planning/phases/04-attorney-practice-area-pages/04-CONTEXT.md (D-15 clearance basis; specifics 64-deal list lines 234-301)
    - .planning/phases/04-attorney-practice-area-pages/04-RESEARCH.md (Validation Architecture lines 489-533 — the test map + Wave 0 gaps)
    - tests/jsonld-legalservice.spec.ts and tests/disclaimer-crawl.spec.ts (test conventions: build-in-beforeAll, read dist/client, cheerio; sitemap walk)
  </read_first>
  <action>
    (a) CLEARANCE: Add to .planning/CLIENT_DISCLOSURE_CLEARANCE.md a single clearing
    entry under Cleared Clients (and a counterparty note row) recording that Jon Van Loo
    cleared Aaron Belcher's COMPLETE representative-transactions list as published at
    https://bsvlaw.com/team/aaron-belcher-partner/ on 2026-05-27, basis = already public
    on the firm's current site (D-15). Per D-15/Assumption A5, reference the bsvlaw.com
    URL as the en-masse basis rather than enumerating all ~60 counterparties individually;
    Allowed contexts = "attorney bio (aaron-belcher); M&A practice-area deal grid". This
    row MUST be committed before Plan 02 publishes Aaron's deals.
    (b) SCAFFOLDS: Create these 8 test files mirroring the build-in-beforeAll + cheerio
    convention. Each that depends on not-yet-existing content uses `test.fixme()` or a
    clearly marked skip with an UNSKIP-WHEN note, so the suite stays green now and the
    later plan un-skips it:
      - tests/person-jsonld.spec.ts (ATTY-09/SEO-03 — assert each built attorney page has a Person JSON-LD block with name/jobTitle/alumniOf/knowsAbout)
      - tests/faqpage-jsonld.spec.ts (PRAC-08/SEO-05 — FAQPage present on practice pages; mainEntity Q/A count == visible <details> count)
      - tests/lint-legal.spec.ts (LEGAL-03 — runs `node scripts/lint-legal.mjs tests/_fixtures/lint-legal-violation.mdx` asserts non-zero; runs clean scan asserts zero) — ACTIVE now
      - tests/fishbien-absent.spec.ts (ATTY-11 — grep dist/client/**/*.html + sitemap + src/content for /fishbien|nir/i, assert zero) — ACTIVE now
      - tests/draft-exclusion.spec.ts (ATTY-06/D-07 — assert no dist/client/attorneys/susan-jiang/index.html and slug absent from sitemap)
      - tests/clearance.spec.ts (LEGAL-04 — for each attorneys MDX representativeDeals with cleared:true, assert clearance basis exists in the register) — can run now (placeholder has none)
      - tests/pages-exist.spec.ts (ATTY-01/PRAC-01 — assert the four published attorney pages + three practice pages exist in dist/client)
      - tests/lead-attorney-link.spec.ts (PRAC-06 — each practice page contains an href to /attorneys/<lead-slug>)
    Also create tests/_fixtures/lint-legal-violation.mdx containing the word "expert" in
    a sentence, with frontmatter, used only by the lint-legal spec.
    Add matching package.json test:* scripts (test:person-jsonld, test:faqpage-jsonld,
    test:lint-legal, test:fishbien, test:draft-exclusion, test:clearance, test:pages-exist,
    test:lead-attorney-link).
  </action>
  <verify>
    <automated>npx playwright test tests/lint-legal.spec.ts tests/fishbien-absent.spec.ts tests/clearance.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: CLIENT_DISCLOSURE_CLEARANCE.md contains "aaron-belcher-partner" and "2026-05-27" with "Jon Van Loo" as clearer.
    - behavior: the three currently-runnable specs (lint-legal, fishbien-absent, clearance) pass; the content-dependent specs are skipped/fixme with UNSKIP-WHEN markers (no red).
    - source: tests/_fixtures/lint-legal-violation.mdx contains the word "expert".
    - test-command: `npm test` is green (skips do not count as failures).
  </acceptance_criteria>
  <done>Aaron's list cleared in the register; 8 test scaffolds + 1 fixture exist; active compliance specs pass; content specs marked for later un-skip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → src/content MDX | Attorney/practice copy authored by humans; banned terms or uncleared names could enter here |
| build script → dist/ | lint:legal + Zod are the build-time gates between authored content and published HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure (Rule 1.6) | clearance register vs. published deals | mitigate | Aaron's list cleared in register BEFORE Plan 02 publishes; tests/clearance.spec.ts cross-checks cleared:true deals against the register |
| T-04-02 | Compliance (Rule 7.4) | lint:legal scanner | mitigate | Real Node scanner over src/content/**/*.mdx; prebuild + CI gate; exits non-zero on non-allowlisted banned root |
| T-04-03 | Tampering / XSS | JSON-LD builders | mitigate | Builders return plain objects; JsonLd.astro (existing) does JSON.stringify escaping — builders never emit raw HTML |
| T-04-04 | Compliance (Rule 7.4 false-positive) | lint allowlist | accept | Allowlist starts empty; "expertise" left banned pending Jon (A1) — over-strict is safe, surfaced for review |
</threat_model>

<verification>
- `npx astro check` clean (jsonld.ts types).
- `node scripts/lint-legal.mjs` exits 0 on current content, 1 on the fixture.
- `npm test` green (active specs pass, content specs skipped).
- Clearance register has Aaron's D-15 row.
</verification>

<success_criteria>
- buildPersonLd + buildFaqPageLd compile and are exported.
- lint:legal is a real scanner wired as prebuild + CI.
- Aaron's 64-deal list cleared (D-15) in the register.
- All 8 Wave 0 test scaffolds + fixture exist; active ones pass.
</success_criteria>

<output>
Create `.planning/phases/04-attorney-practice-area-pages/04-01-SUMMARY.md` when done
</output>
