---
phase: 04-attorney-practice-area-pages
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/content/practiceAreas/mergers-acquisitions.mdx
  - src/content/practiceAreas/intellectual-property-technology-transactions.mdx
  - src/content/practiceAreas/tax.mdx
  - src/content/testimonials/daniel-brian.mdx
  - src/content/practiceAreas/placeholder-practice.mdx
  - src/content/testimonials/placeholder.mdx
  - src/layouts/PracticeAreaLayout.astro
  - .planning/phases/04-attorney-practice-area-pages/FAQ-DRAFT.md
autonomous: true
requirements: [PRAC-01, PRAC-02, PRAC-03, PRAC-04, PRAC-05, PRAC-06, PRAC-07, PRAC-09, SEO-05, LEGAL-06, LEGAL-08, LEGAL-10]
must_haves:
  truths:
    - "A visitor can open /practice-areas/mergers-acquisitions, /practice-areas/intellectual-property-technology-transactions, /practice-areas/tax"
    - "Each page opens with the client's problem, then BSV's team solution, then proof (lead-partner callout linking to the profile), then fee band"
    - "The M&A page renders a cleared deal grid + the Daniel Brian testimonial with the California disclosure; IP and Tax have no deal grid and no testimonial"
    - "Each practice page carries the practice-area disclaimer; FAQPage JSON-LD is wired but guarded (no FAQs committed yet)"
    - "Draft FAQs (3-5 per area) are written to FAQ-DRAFT.md for Jon's review — NOT into MDX"
  artifacts:
    - path: "src/content/practiceAreas/mergers-acquisitions.mdx"
      provides: "M&A page data: clientProblem, bsvApproach, cleared deal strings, leadAttorneys [aaron-belcher]"
      contains: "aaron-belcher"
    - path: "src/layouts/PracticeAreaLayout.astro"
      provides: "lead-attorney getEntries resolution + FAQPage slot (guarded) + section composition"
      contains: "getEntries"
    - path: ".planning/phases/04-attorney-practice-area-pages/FAQ-DRAFT.md"
      provides: "3-5 reviewed-pending FAQs per practice area for the D-13 gate"
      contains: "M&A"
  key_links:
    - from: "src/layouts/PracticeAreaLayout.astro"
      to: "src/content/attorneys"
      via: "getEntries(practiceArea.data.leadAttorneys) → /attorneys/<slug> link"
      pattern: "getEntries"
    - from: "src/layouts/PracticeAreaLayout.astro"
      to: "src/lib/jsonld.ts"
      via: "buildFaqPageLd guarded on faqs.length"
      pattern: "buildFaqPageLd"
---

<objective>
Ship the practice-area vertical slice (minus the human-gated FAQ copy): replace
the placeholder practice MDX with three real pages, create the Daniel Brian
testimonial entry, and render PracticeAreaLayout — problem → BSV solution →
proof (lead-partner callout that resolves the reference and links to the
profile; M&A also gets the cleared deal grid + testimonial with CA disclosure) →
fee band → practice-area disclaimer. FAQPage JSON-LD is wired but guarded on
empty `faqs` (populated in Plan 04 after Jon approves). Draft the FAQs to a
review file for the D-13 gate.

Purpose: After this plan all three practice pages are live and compliant
(ROADMAP success criteria 2, 3, 5) and the FAQ drafts are staged for Jon.
Output: 3 practice MDX + 1 testimonial MDX + rendered PracticeAreaLayout +
FAQ-DRAFT.md; placeholders removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-attorney-practice-area-pages/04-CONTEXT.md
@.planning/phases/04-attorney-practice-area-pages/04-PATTERNS.md
@.planning/FIRM_BRIEF.md
@.planning/CLIENT_DISCLOSURE_CLEARANCE.md
@.planning/phases/04-attorney-practice-area-pages/04-01-SUMMARY.md

<interfaces>
<!-- Contracts extracted from the codebase. Use directly. -->

From src/content.config.ts (practiceAreas schema):
- name, slug, summary, clientProblem, bsvApproach (all required strings),
  representativeDeals:string[]=[], leadAttorneys: reference('attorneys')[].min(1) (REQUIRED),
  faqs:{question,answer}[]=[], feeStructureBand:boolean=true, icon?:image(), order, draft=false
From src/content.config.ts (testimonials schema):
- quote, attribution, role (all required), matter?, practiceArea?: reference('practiceAreas'), featured=false

From src/content/practiceAreas/placeholder-practice.mdx + src/content/testimonials/placeholder.mdx (exact-match analogs).

From src/layouts/PracticeAreaLayout.astro (the file being edited — reserved slot line 22, Disclaimer line 29):
- Props `{ practiceArea: CollectionEntry<'practiceAreas'> }`; renders <BaseLayout>, <h1>{name}</h1>, <slot/>, <Disclaimer id="practice-area" />.

Cross-collection resolution (PATTERNS lines 189-198): leadAttorneys is reference objects, NOT data.
  `import { getEntries } from 'astro:content'; const leads = await getEntries(practiceArea.data.leadAttorneys);`
  then leads[i].data.name + href={`/attorneys/${leads[i].data.slug}`}. Rendering raw prints [object Object].

Component prop shapes (compose in layout, adapters per PATTERNS Shared Patterns):
- DealsGrid: deals:{title,amount?,context?}[] — practiceAreas.representativeDeals is string[] → map(s => ({title:s})).
- TestimonialQuote: quote, attribution, attributionDetail? + a `disclosure` named slot; testimonials role→attributionDetail; put CA disclosure in <p slot="disclosure">.
- FeeStructureBand: heading, body, note? — ONE consistent band across all 3 pages, hourly + estimate, NO published rate (PRAC-09/LEGAL-08).
- FaqAccordion: items:{q,a}[] — adapter faqs.map(f=>({q:f.question,a:f.answer})). (No FAQs to render yet — guard; populated Plan 04.)
- JsonLd: data:object, slot="head". buildFaqPageLd from src/lib/jsonld.ts (Plan 01).

CA testimonial disclosure wording (RESEARCH/PATTERNS — Jon confirms at review):
  "This testimonial reflects one client's experience and is not a guarantee of any future result. Prior results do not guarantee a similar outcome."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the three practice MDX + Daniel Brian testimonial; remove placeholders</name>
  <read_first>
    - src/content/practiceAreas/placeholder-practice.mdx (frontmatter-key analog)
    - src/content/testimonials/placeholder.mdx (testimonial analog)
    - src/content.config.ts (practiceAreas + testimonials schemas)
    - .planning/phases/04-attorney-practice-area-pages/04-CONTEXT.md (D-09 M&A deals+testimonial; D-10 IP/Tax no grid/no testimonial; D-11 lead attorneys)
    - .planning/CLIENT_DISCLOSURE_CLEARANCE.md (which deal names are cleared for the M&A grid)
    - .planning/FIRM_BRIEF.md (positioning, problems, fee structure, Daniel Brian quote line 156-157)
  </read_first>
  <action>
    Create three practiceAreas MDX replacing the placeholder, with `faqs: []` for now
    (D-13 — FAQ copy is committed in Plan 04 only after Jon approves):
      - mergers-acquisitions.mdx: slug "mergers-acquisitions", clientProblem + bsvApproach
        (StoryBrand: client's complex-transaction problem → BSV's team-driven M&A solution),
        representativeDeals = a CURATED set of cleared M&A deal strings drawn from Aaron's list
        / homepage-cleared deals (e.g. "Athelas merged with Commure in $6 billion merger",
        "Thoughtspot acquired Mode Analytics in $200 million transaction", "Illumina defended
        against Roche's $6.4 billion hostile pursuit") — ONLY names present in the clearance
        register (PRAC-05/LEGAL-04). leadAttorneys ["aaron-belcher"] (D-11). feeStructureBand true, order 1.
      - intellectual-property-technology-transactions.mdx: slug per URL-CONVENTIONS,
        clientProblem + bsvApproach for IP/tech, representativeDeals [] (D-10 — NO grid),
        leadAttorneys ["stuart-smolen"] (D-11), feeStructureBand true, order 2.
      - tax.mdx: slug "tax", clientProblem + bsvApproach for tax/crypto-tax, representativeDeals []
        (D-10), leadAttorneys ["jon-van-loo"] (D-11), feeStructureBand true, order 3.
    Create src/content/testimonials/daniel-brian.mdx: quote "BSV was my rock throughout the
    $6 billion merger between Athelas and Commure.", attribution "Daniel Brian", role "GC, Commure, Inc.",
    matter "Athelas–Commure merger", practiceArea "mergers-acquisitions", featured true.
    LEGAL-10: no outcome/guarantee/results language in clientProblem/bsvApproach prose.
    DELETE src/content/practiceAreas/placeholder-practice.mdx and src/content/testimonials/placeholder.mdx.
  </action>
  <verify>
    <automated>npx astro check && npm run lint:legal</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `npx astro check` passes (3 practice MDX + testimonial satisfy schemas; leadAttorneys references resolve to existing attorney slugs from Plan 02).
    - behavior: `npm run lint:legal` exits 0.
    - source: mergers-acquisitions.mdx leadAttorneys contains "aaron-belcher" and representativeDeals contains only register-cleared names; IP and Tax MDX have representativeDeals [].
    - source: daniel-brian.mdx exists with the verbatim quote; placeholders removed.
  </acceptance_criteria>
  <done>Three practice pages + Daniel Brian testimonial validate and pass lint; M&A has cleared deals, IP/Tax have none; placeholders removed.</done>
</task>

<task type="auto">
  <name>Task 2: Render PracticeAreaLayout (problem→solution→proof→fee band, guarded FAQPage) + draft FAQs to review file</name>
  <read_first>
    - src/layouts/PracticeAreaLayout.astro (the file being edited — reserved slot line 22, Disclaimer line 29)
    - src/components/sections/{DealsGrid,TestimonialQuote,FeeStructureBand,FaqAccordion,CtaBlock}.astro (prop shapes)
    - src/lib/jsonld.ts (buildFaqPageLd — Plan 01)
    - src/pages/design-system.astro (composition reference lines 194-220; fee band copy 205-209)
    - .planning/phases/04-attorney-practice-area-pages/04-PATTERNS.md (PracticeAreaLayout section lines 179-201; Shared Patterns adapters lines 263-312)
    - .planning/phases/04-attorney-practice-area-pages/04-RESEARCH.md (AEO/FAQ authoring guidance lines 568-581 — candidate questions; Rule 7.1 guardrails)
  </read_first>
  <action>
    Edit src/layouts/PracticeAreaLayout.astro. Import getEntries (astro:content), JsonLd,
    buildFaqPageLd, getCollection (for the testimonial), and the section components.
    Resolve lead attorneys: `const leads = await getEntries(practiceArea.data.leadAttorneys);`
    Render a lead-partner callout that links each lead to `/attorneys/${lead.data.slug}` (PRAC-06).
    Compose the page in StoryBrand order: clientProblem (problem) → bsvApproach (solution) →
    <slot/> MDX narrative → proof: lead-partner callout, and IF representativeDeals.length>0
    render <DealsGrid deals={representativeDeals.map(s=>({title:s}))} heading="Representative transactions" />
    (M&A only). IF a testimonial exists for this practiceArea (query testimonials collection,
    M&A only), render <TestimonialQuote> with the CA disclosure in <p slot="disclosure"> (LEGAL-06/D-18).
    IF feeStructureBand, render ONE consistent <FeeStructureBand heading="How we bill" .../> — hourly +
    up-front estimate, NO published rate (PRAC-09/LEGAL-08). Close with a <CtaBlock ctaHref="/contact" />.
    FAQPage slot: replace the line-22 comment with a GUARDED emission —
    `{practiceArea.data.faqs.length > 0 && <JsonLd slot="head" data={buildFaqPageLd(practiceArea.data.faqs)} />}`
    and render <FaqAccordion items={faqs.map(f=>({q:f.question,a:f.answer}))} /> only when faqs.length>0
    (SEO-05/PRAC-08 — wired now, populated Plan 04). Keep <Disclaimer id="practice-area" /> (PRAC-07/LEGAL-02).
    Token-only utilities; never hardcoded hex. Do NOT touch the [slug].astro route.
    Then DRAFT the FAQs (do NOT write them into MDX): create
    .planning/phases/04-attorney-practice-area-pages/FAQ-DRAFT.md with 3-5 plain-English,
    2-3-sentence, non-promissory FAQs per practice area (M&A, IP & Technology, Tax) using the
    RESEARCH candidate questions, mapped to Jon's/Stuart's focus areas. Rule 7.1/7.4/LEGAL-10
    guardrails: no expert/specialist/specialize, no outcome guarantees, no "results". The "how
    much does it cost" answer must align with the FeeStructureBand copy (hourly + estimate, no rate).
    Mark the file "PENDING JON'S APPROVAL — D-13 gate (Plan 04)".
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/pages-exist.spec.ts tests/lead-attorney-link.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - behavior: build emits dist/client/practice-areas/{mergers-acquisitions,intellectual-property-technology-transactions,tax}/index.html.
    - behavior: tests/lead-attorney-link.spec.ts passes — each practice page links to its lead attorney's /attorneys/<slug>.
    - behavior: the M&A page HTML contains a DealsGrid and the Daniel Brian quote + the CA disclosure string; IP and Tax pages contain neither a DealsGrid nor a testimonial.
    - source: PracticeAreaLayout.astro contains `getEntries` and a `faqs.length` guard around buildFaqPageLd; no hardcoded hex.
    - source: FAQ-DRAFT.md exists with FAQs for all three areas and the PENDING APPROVAL marker; no FAQs were written into any MDX (faqs:[] still).
  </acceptance_criteria>
  <done>Three practice pages render problem→solution→proof→fee band with disclaimer; M&A deal grid + disclosed testimonial; FAQPage guarded; FAQ drafts staged for Jon (not committed to MDX).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| practice MDX → rendered page | Deal strings + problem/solution copy cross into public HTML |
| leadAttorneys reference → profile link | Cross-collection link must resolve, not leak [object Object] |
| testimonial → public quote | Client testimonial requires CA disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure (Rule 1.6) | M&A deal grid names | mitigate | Only register-cleared names used (Plan 01 D-15); tests/clearance.spec.ts + lint review |
| T-04-11 | Compliance (Rule 7.1 / LEGAL-06) | Daniel Brian testimonial | mitigate | CA disclosure rendered via TestimonialQuote disclosure slot (D-18); attribution cleared in register |
| T-04-12 | Compliance (Rule 7.1 / LEGAL-10) | problem/solution + FAQ drafts | mitigate | FAQs staged to FAQ-DRAFT.md behind Jon's D-13 gate; non-promissory guardrails; lint:legal on prose |
| T-04-13 | Compliance (Rule 7.4) | practice copy | mitigate | npm run lint:legal build gate (no specialist/expert/specialize) |
| T-04-14 | Tampering / XSS | FAQ/deal strings in JSON-LD | accept | JsonLd.astro JSON.stringify escaping; FAQPage guarded on empty |
</threat_model>

<verification>
- `npx astro check` + `npm run lint:legal` clean.
- `npm run build` emits 3 practice pages.
- pages-exist + lead-attorney-link specs pass.
- M&A has grid + disclosed testimonial; IP/Tax do not.
- FAQ-DRAFT.md staged, no FAQs in MDX yet.
</verification>

<success_criteria>
- All three practice pages render problem→solution→proof→fee band + disclaimer.
- M&A deal grid (cleared) + Daniel Brian testimonial with CA disclosure; IP/Tax neither.
- Lead-partner callouts link to profiles; FAQPage JSON-LD wired and guarded.
- FAQ drafts staged for Jon's approval (D-13), not yet committed to MDX.
</success_criteria>

<output>
Create `.planning/phases/04-attorney-practice-area-pages/04-03-SUMMARY.md` when done
</output>
