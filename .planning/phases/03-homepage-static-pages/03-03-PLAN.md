---
phase: 03-homepage-static-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/attorneys/index.astro
  - src/pages/practice-areas/index.astro
  - src/pages/blog/index.astro
  - src/pages/404.astro
  - src/pages/500.astro
autonomous: true
requirements: [PAGES-04, PAGES-05, PAGES-06, PAGES-07, SEO-01, SEO-02, SEO-07, SEO-08]
must_haves:
  truths:
    - "From the homepage a visitor reaches the Practice Areas index, the Attorneys index, and the Insights index in one click each"
    - "The Practice Areas index lists all three practice areas with links to /practice-areas/[slug]; the Attorneys index lists all five attorneys (never Fishbien)"
    - "The Insights index handles the empty state gracefully — branded, on-tone, not an error"
    - "A nonexistent URL or server error shows a branded 404 / 5xx page with a path home and to /contact, not a generic Vercel page"
    - "sitemap.xml and robots.txt are live and LegalService JSON-LD is present site-wide via BaseLayout; every page sets title/description/canonical/OG via SeoHead"
  artifacts:
    - path: "src/pages/practice-areas/index.astro"
      provides: "Practice Areas index listing all three areas with slug links"
      contains: "mergers-acquisitions"
    - path: "src/pages/attorneys/index.astro"
      provides: "Attorneys index listing all five attorneys"
      min_lines: 15
    - path: "src/pages/blog/index.astro"
      provides: "Insights index with graceful branded empty-state"
      contains: "length === 0"
    - path: "src/pages/404.astro"
      provides: "Branded 404 with path home and to contact"
      contains: "/contact"
    - path: "src/pages/500.astro"
      provides: "Branded 5xx with SITE.email and path home"
      contains: "SITE.email"
  key_links:
    - from: "src/pages/practice-areas/index.astro"
      to: "/practice-areas/[slug]"
      via: "card/link hrefs to locked slugs"
      pattern: "/practice-areas/(mergers-acquisitions|tax|intellectual-property)"
    - from: "src/pages/404.astro"
      to: "/contact"
      via: "path-back link / Button"
      pattern: "/contact"
---

<objective>
Deliver the section-index and error-page slice — the navigational spine that lets a visitor reach every section in one click and never lands on a generic Vercel page — plus confirm the SEO/sitemap/robots/JSON-LD baseline is live across the static surface.

Purpose: Close the StoryBrand navigation loop (Practice Areas, Attorneys, Insights indexes), give the Insights index a warm empty state, brand the 404/5xx pages with a clear path back, and verify the SEO plumbing (SeoHead meta, site-wide LegalService JSON-LD, sitemap.xml, robots.txt) covers the new public pages.
Output: Three retrofitted, real-looking section indexes; two branded error pages; verified sitemap/robots/JSON-LD/meta baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-homepage-static-pages/03-CONTEXT.md
@.planning/phases/03-homepage-static-pages/03-PATTERNS.md
@.planning/URL-CONVENTIONS.md

<interfaces>
<!-- Index/error page shapes. Extracted from the existing placeholder pages + section components. -->

Collection-read pattern (keep): `const items = await getCollection('<name>', ({ data }) => !data.draft)` then a `items.length === 0` empty-state branch.
  NOTE: collections currently hold only placeholder MDX — use the SAME curated `attorneys`/practice-teaser data the homepage uses (design-system.astro consts) to render real-looking cards, OR map collection entries; planner's call. Either way keep the `length === 0` graceful branch.

PracticeAreaCard: { title, blurb, icon (astro-icon name), href } — all required; grid idiom `grid gap-8 sm:grid-cols-2 lg:grid-cols-3`
AttorneyCard: { name, role, focus, photo (ImageMetadata — imported asset), alt, href? } — omit href in Phase 3 (non-linking until Phase 4 detail routes)
Button: { variant: "primary"|"secondary", href, label }
SITE (src/lib/site.ts): { email, ... } — 500.astro surfaces SITE.email
BaseLayout props: { title (req), description (req), canonical?, ogImage?, noindex? } — do NOT pass noindex on any of these public pages; site-wide LegalService JSON-LD is auto-injected by BaseLayout; SeoHead emits title/description/canonical/OG.

Locked slugs (URL-CONVENTIONS.md): practice areas = mergers-acquisitions, intellectual-property-technology-transactions, tax; attorneys = aaron-belcher, stuart-smolen, jon-van-loo, iris-zhang, susan-jiang.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render real-looking Practice Areas and Attorneys index pages</name>
  <read_first>
    - src/pages/practice-areas/index.astro (the file being modified — keep its getCollection + length===0 structure; retrofit tokens)
    - src/pages/attorneys/index.astro (the file being modified — same treatment; uses entry.data.slug / entry.data.name)
    - src/pages/design-system.astro (the curated `attorneys` const + 5 headshot imports + the 3 practice teaser cards with locked slugs — content source if rendering curated cards)
    - src/components/sections/PracticeAreaCard.astro and src/components/sections/AttorneyCard.astro (prop shapes; AttorneyCard renders a non-linking <div> when href omitted)
    - .planning/URL-CONVENTIONS.md (the locked practice-area + attorney slugs the links must follow)
    - .planning/phases/03-homepage-static-pages/03-PATTERNS.md (the index pattern assignments + the curated-data-vs-collection note)
  </read_first>
  <action>
    Rewrite `src/pages/practice-areas/index.astro` (PAGES-04): keep the `getCollection('practiceAreas', ({data}) => !data.draft)` read AND a graceful `length === 0` branch, but render real-looking cards so the preview looks complete — render three `PracticeAreaCard` (the same icon/title/blurb/href set used on the homepage: hrefs `/practice-areas/mergers-acquisitions`, `/practice-areas/intellectual-property-technology-transactions`, `/practice-areas/tax`) in the `grid gap-8 sm:grid-cols-2 lg:grid-cols-3` layout, inside a `mx-auto max-w-6xl px-gutter py-section` container with an `<h1 class="text-h1 font-bold text-text">` heading. Links resolve to the Phase 4 detail routes (acceptable to be interim-dead per CONTEXT). Rewrite `src/pages/attorneys/index.astro` (PAGES-05): keep the `getCollection('attorneys', ...)` read + `length === 0` branch; render all five attorneys as `AttorneyCard` (reuse the curated `attorneys` const + the five headshot imports from design-system.astro) with NO `href` (non-linking in Phase 3), in the `grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5` layout — Belcher, Smolen, Van Loo, Zhang, Jiang only; Fishbien appears nowhere. Retrofit BOTH files from the old `text-3xl ... text-[color:var(--color-text)]` form to the namespace tokens (`text-h1 font-bold text-text`, `text-body text-text-muted`). Keep the public BaseLayout wrappers (no noindex) with their existing title/description props.
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "error" | Select-Object -First 3; node -e "const pa=require('fs').readFileSync('src/pages/practice-areas/index.astro','utf8'); const at=require('fs').readFileSync('src/pages/attorneys/index.astro','utf8'); const okSlugs=['mergers-acquisitions','intellectual-property-technology-transactions','tax'].every(s=>pa.includes(s)); const okPeople=['Belcher','Smolen','Van Loo','Zhang','Jiang'].every(s=>at.includes(s)); const noFish=!at.includes('Fishbien'); const noOld=!pa.includes('text-[color:var(') && !at.includes('text-[color:var('); process.exit(okSlugs&&okPeople&&noFish&&noOld?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/practice-areas/index.astro` renders three PracticeAreaCards linking to the three locked slugs and retains a `length === 0` empty-state branch
    - `src/pages/attorneys/index.astro` renders five non-linking AttorneyCards (Belcher, Smolen, Van Loo, Zhang, Jiang) and contains no "Fishbien"
    - Neither index contains `text-[color:var(` (token retrofit complete)
    - Both keep the public BaseLayout wrapper (no `noindex`)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Both section indexes render real, on-brand cards (so the preview looks real), follow the locked slugs, exclude Fishbien, retain graceful empty-state branches, and are token-retrofitted.</done>
</task>

<task type="auto">
  <name>Task 2: Warm the Insights index empty-state and brand the 404 / 500 pages</name>
  <read_first>
    - src/pages/blog/index.astro (the file being modified — already the canonical empty-state pattern at lines 14-17; retrofit tokens + warm the copy)
    - src/pages/404.astro (the file being modified — already links home; add a /contact path, retrofit tokens, warm the copy)
    - src/pages/500.astro (the file being modified — already imports SITE + surfaces SITE.email; retrofit tokens, warm the copy)
    - src/components/ui/Button.astro (the path-back CTA primitive — variant/href/label)
    - .planning/phases/03-homepage-static-pages/03-CONTEXT.md (Insights empty-state = branded/on-tone/not-an-error; 404/5xx = warm copy + path home AND to contact)
  </read_first>
  <action>
    Rewrite `src/pages/blog/index.astro` (PAGES-06): keep the `getCollection('blog', ...)` read and the `posts.length === 0` branch, but warm the empty-state copy on-tone (e.g. "Insights are on the way — practical analysis from the BSV team on M&A, IP, and tax. Check back soon.") inside a `mx-auto max-w-3xl px-gutter py-section` container with an `<h1 class="text-h1 font-bold text-text">Insights</h1>` and a `<Button>` back to `/` or to `/contact`. Branded, on-tone, NOT an error. Retrofit tokens. Rewrite `src/pages/404.astro` (PAGES-07): warm on-tone copy ("This page took a wrong turn..."), an `<h1 class="text-h1 font-bold text-text">`, and TWO clear paths — a primary `<Button variant="primary" href="/" label="Back to home" />` AND a secondary `<Button variant="secondary" href="/contact" label="Get in touch" />`. Retrofit the old `text-3xl ... text-[color:var(...)]` to namespace tokens. Rewrite `src/pages/500.astro` (PAGES-07): keep the `import { SITE }` and the `SITE.email` mailto, warm the copy, retrofit tokens, add a path home (`<Button href="/" .../>`). NEITHER error page passes `noindex` (they are normal routes replacing the generic Vercel pages). Keep public BaseLayout wrappers.
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "error" | Select-Object -First 3; node -e "const b=require('fs').readFileSync('src/pages/blog/index.astro','utf8'); const f4=require('fs').readFileSync('src/pages/404.astro','utf8'); const f5=require('fs').readFileSync('src/pages/500.astro','utf8'); const okEmpty=b.includes('length === 0')||b.includes('posts.length'); const ok404=f4.includes('/contact')&&!f4.includes('text-[color:var('); const ok500=f5.includes('SITE.email')&&!f5.includes('text-[color:var('); const okBlogTok=!b.includes('text-[color:var('); process.exit(okEmpty&&ok404&&ok500&&okBlogTok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/blog/index.astro` retains the `posts.length === 0` branch with warm, branded on-tone copy (not an error message)
    - `src/pages/404.astro` offers a path home AND a path to `/contact` and contains no `text-[color:var(`
    - `src/pages/500.astro` keeps `SITE.email` and a path home and contains no `text-[color:var(`
    - Neither error page passes `noindex`
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>The Insights index shows a graceful branded empty-state and both error pages are warm, on-tone, token-retrofitted, and give clear paths back home and to contact.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the SEO / sitemap / robots / JSON-LD baseline across the static surface</name>
  <read_first>
    - src/layouts/BaseLayout.astro (confirm SeoHead emits title/description/canonical/OG and that buildLegalServiceLd() JSON-LD injects site-wide at line 46)
    - src/components/seo/SeoHead.astro (confirm the meta tags emitted)
    - tests/jsonld-legalservice.spec.ts and tests/disclaimer-crawl.spec.ts (the existing tests that run a fresh build and assert JSON-LD + footer disclaimer across sitemap routes)
    - .planning/URL-CONVENTIONS.md (the canonical route list — sitemap.xml + robots.txt are listed deliverables)
  </read_first>
  <action>
    This is a verification + meta-hygiene task (SEO-01/02/07/08), not a redesign. (1) Confirm every Phase 3 public page passes a real `title` and `description` to BaseLayout (SeoHead emits title/description/canonical/OG) — fix any page still using the placeholder copy so titles/descriptions are page-appropriate (homepage, about, the three indexes, 404, 500). (2) Confirm BaseLayout still injects the site-wide `LegalService` JSON-LD on every page (SEO-02) — do NOT add per-page JSON-LD here (attorney/practice/article schema is Phase 4/5). (3) After a full `npm run build`, confirm `dist/client/` contains a sitemap (`sitemap-index.xml` + `sitemap-0.xml`, or `sitemap.xml`) listing the new public routes (`/about`, `/practice-areas`, `/attorneys`, `/blog`) and a `robots.txt` referencing the sitemap (SEO-07/08). If `robots.txt` is missing from the build output, add the minimal robots generation consistent with the existing astro.config sitemap setup (do not loosen the noindex/sitemap-exclusion discipline for `/design-system`). Make no change that would re-index the hidden gallery. (4) Run the existing `npm run test:jsonld` and `npm run test:disclaimer` and confirm both pass against the now-populated pages.
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "error" | Select-Object -First 3; node -e "const fs=require('fs'); const hasSitemap=['dist/client/sitemap-index.xml','dist/client/sitemap-0.xml','dist/client/sitemap.xml'].some(p=>fs.existsSync(p)); const hasRobots=fs.existsSync('dist/client/robots.txt'); process.exit(hasSitemap&&hasRobots?0:1)"; npm run test:jsonld 2>&1 | Select-String -Pattern "passed|failed" | Select-Object -First 2; npm run test:disclaimer 2>&1 | Select-String -Pattern "passed|failed" | Select-Object -First 2</automated>
  </verify>
  <acceptance_criteria>
    - Each Phase 3 public page passes a page-appropriate `title` + `description` to BaseLayout (no leftover placeholder copy in titles/descriptions)
    - `astro build` produces a sitemap (`sitemap-index.xml`/`sitemap-0.xml` or `sitemap.xml`) and a `robots.txt` in `dist/client/` (SEO-07/08)
    - `robots.txt` references the sitemap and does NOT expose `/design-system`
    - `npm run test:jsonld` passes — LegalService JSON-LD present site-wide via BaseLayout (SEO-02)
    - `npm run test:disclaimer` passes — footer disclaimer present on every sitemap route
  </acceptance_criteria>
  <done>Every static page carries proper SEO meta, the site-wide LegalService JSON-LD is confirmed present, and sitemap.xml + robots.txt are live in the build output with the hidden gallery still excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time content -> static HTML | Index/error pages render from curated data + collection reads at build time; no user input, no form, no DB. 404/500 are static routes. |

## STRIDE Threat Register (ASVS L1, proportionate to read-only static content)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information disclosure | Branded 404 / 500 pages | mitigate | Error pages render fixed warm copy + SITE.email + paths home/contact — they MUST NOT leak stack traces, internal paths, or env values (Task 2 uses static content only) |
| T-03-08 | Information disclosure | sitemap.xml / robots.txt | mitigate | The hidden `/design-system` gallery stays noindex + sitemap-excluded (D-16/D-19); robots.txt must not advertise it (Task 3 assertion) |
| T-03-09 | Tampering | CSP — no inline/third-party script | mitigate | Index/error pages add no inline `<script>` and no third-party embed; locked CSP (`font-src 'self'`) not loosened |
| T-03-10 | Information disclosure | JSON-LD | accept/N-A | LegalService JSON-LD is serialized data injected by BaseLayout; no new per-page JSON-LD added here |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs NO packages — composes existing components; robots generation (if needed) uses the already-installed sitemap integration. No package-legitimacy checkpoint required |
| (auth/session/input L1) | S/R/E | — | N/A | No authentication, sessions, forms, or user input on these pages |
</threat_model>

<verification>
- `npm run build` succeeds and emits HTML for `/about`, `/practice-areas`, `/attorneys`, `/blog`, `/404`, `/500` plus sitemap + robots.txt.
- `npm run test:jsonld` and `npm run test:disclaimer` pass.
- Practice Areas index links resolve to the three locked slugs; Attorneys index excludes Fishbien.
</verification>

<success_criteria>
- Practice Areas index lists all three areas with slug links (PAGES-04)
- Attorneys index lists all five attorneys, Fishbien excluded (PAGES-05)
- Insights index handles the empty state gracefully (PAGES-06)
- Branded 404 + 5xx pages with paths home and to contact (PAGES-07)
- SeoHead meta on every page (SEO-01); site-wide LegalService JSON-LD (SEO-02); sitemap.xml (SEO-07); robots.txt referencing the sitemap (SEO-08)
</success_criteria>

<output>
Create `.planning/phases/03-homepage-static-pages/03-03-SUMMARY.md` when done.
</output>
