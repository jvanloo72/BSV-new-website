---
phase: 03-homepage-static-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/CLIENT_DISCLOSURE_CLEARANCE.md
  - .planning/DECISIONS.md
  - src/components/sections/ApproachBand.astro
  - src/components/sections/ChambersStrip.astro
  - src/pages/index.astro
autonomous: true
requirements: [PAGES-01, PAGES-02, PAGES-08, LEGAL-07]
must_haves:
  truths:
    - "The homepage headline reads 'Team work to get good results.' (not credentials or awards)"
    - "A homepage visitor sees three practice-area teaser cards, all five attorneys (Belcher, Smolen, Van Loo, Zhang, Jiang — never Fishbien), a marquee testimonial, the Chambers Spotlight 2026 strip, and a primary CTA toward /contact"
    - "The homepage's named deals are recorded as cleared in CLIENT_DISCLOSURE_CLEARANCE.md before they appear publicly"
    - "The BSV Approach band below the hero maps three points to competence / experience / responsiveness"
  artifacts:
    - path: ".planning/CLIENT_DISCLOSURE_CLEARANCE.md"
      provides: "D-04 cleared deals recorded before homepage publishes them"
      contains: "Athelas"
    - path: "src/components/sections/ApproachBand.astro"
      provides: "StoryBrand guide-with-a-plan band: lead line + 3 points (competence/experience/responsiveness)"
      min_lines: 20
    - path: "src/components/sections/ChambersStrip.astro"
      provides: "Quiet accent-tinted Chambers USA Spotlight 2026 recognition strip"
      min_lines: 10
    - path: "src/pages/index.astro"
      provides: "Homepage composition in D-01 order"
      contains: "Team work to get good results"
  key_links:
    - from: "src/pages/index.astro"
      to: "/contact"
      via: "Hero ctaHref + CtaBlock ctaHref"
      pattern: "ctaHref=\"/contact\""
    - from: "src/pages/index.astro"
      to: "src/components/sections/ApproachBand.astro"
      via: "import + render after Hero"
      pattern: "ApproachBand"
    - from: "src/pages/index.astro"
      to: "src/components/sections/ChambersStrip.astro"
      via: "import + render before CtaBlock"
      pattern: "ChambersStrip"
---

<objective>
Deliver the homepage as a complete, shippable vertical slice — the highest-value page in the phase — and clear the legal gate that must precede publishing its named deals.

Purpose: A prospective client lands on `/`, reads "Team work to get good results," moves through the BSV Approach beat (the StoryBrand guide-with-a-plan), scans practice teasers, sees the representative-work proof, the five-attorney row, a marquee testimonial, the Chambers recognition, and is pulled toward `/contact`.
Output: Populated clearance register; two new small section components (`ApproachBand`, `ChambersStrip`); a fully composed public homepage in the locked D-01 order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage-static-pages/03-CONTEXT.md
@.planning/phases/03-homepage-static-pages/03-PATTERNS.md
@.planning/CLIENT_DISCLOSURE_CLEARANCE.md

<interfaces>
<!-- Section component prop shapes the homepage composes. Extracted from src/components/sections/. Use directly — no exploration needed. -->

Hero: { headline (req), subhead?, eyebrow?, ctaLabel (req), ctaHref (req), secondaryLabel?, secondaryHref? }
PracticeAreaCard: { title, blurb, icon (astro-icon name, e.g. "practice-tax"), href } — all required
AttorneyCard: { name, role, focus, photo (ImageMetadata — imported asset), alt, href? } — omit href in Phase 3 so it renders a non-linking <div> with no "View profile"
TestimonialQuote: { quote, attribution (req), attributionDetail? }
DealsGrid: { deals: { title; amount?; context? }[], heading? }
CtaBlock: { heading, ctaLabel (req), ctaHref (req), body? }

Eyebrow: slot-only kicker -> renders `text-small uppercase tracking-wide text-text-muted`
FeeStructureBand (analog for ApproachBand only — NOT used on homepage): banded surface with accent marker `<span class="block h-1 w-10 rounded-full bg-accent" aria-hidden="true"></span>`

BaseLayout props: { title (req), description (req), canonical?, ogImage?, noindex? }
  — BaseLayout auto-injects site-wide LegalService JSON-LD (line 46) and the SiteFooter Disclaimer. Do NOT pass noindex on the homepage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate the client-disclosure clearance register (D-04 legal gate)</name>
  <read_first>
    - .planning/CLIENT_DISCLOSURE_CLEARANCE.md (the file being modified — current empty template; note the three tables: Cleared Clients, Cleared Counterparties, Cleared Deal Codenames, and the Process section)
    - .planning/phases/03-homepage-static-pages/03-CONTEXT.md (D-04 — exact cleared items, attribution, and that this UNBLOCKS the Phase 4 gate)
    - .planning/DECISIONS.md (append target — follow the existing entry format)
  </read_first>
  <action>
    Edit `.planning/CLIENT_DISCLOSURE_CLEARANCE.md` to record the homepage's named deals as cleared by Jon Van Loo on 2026-05-26 (per D-04). In the "Cleared Clients" table add rows for: Athelas (allowed context "practice-area deal grid; homepage representative-work strip; testimonial"), Commure (same context), Mode Analytics ("homepage representative-work strip; practice-area deal grid"), Illumina ("homepage representative-work strip; practice-area deal grid"). In "Cleared Counterparties" add Roche (counterparty in the Illumina defense — public-record hostile bid). Add a representative-parties note row covering Adobe, Oracle, PayPal, Dell, eBay, Coinbase (allowed context "homepage representative-parties strip; attorney bio"). In the Daniel Brian testimonial — record Daniel Brian (GC, Commure, Inc.) and the verbatim quote "BSV was my rock throughout the $6 billion merger between Athelas and Commure." as cleared for the "testimonial" context. Every added row MUST fill `Cleared by` = "Jon Van Loo" and `Cleared on` = "2026-05-26". Then append one entry to `.planning/DECISIONS.md` recording the D-04 clearance (what: homepage deals cleared; why: most are public, approving-partner sign-off; teaching insight: the clearance register is the structural Rule 1.6/7.4 gate that prevents an uncleared client name from shipping). Do NOT remove existing template prose, the Process section, or the "(none yet)" guidance rows below the data.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/CLIENT_DISCLOSURE_CLEARANCE.md','utf8'); const ok=['Athelas','Daniel Brian','Jon Van Loo','2026-05-26','Illumina','Mode Analytics'].every(s=>f.includes(s)); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/CLIENT_DISCLOSURE_CLEARANCE.md` contains "Athelas", "Mode Analytics", "Illumina", "Daniel Brian"
    - Every newly added clearance row has `Cleared by` = "Jon Van Loo" and `Cleared on` = "2026-05-26"
    - `.planning/DECISIONS.md` gains one new entry referencing D-04 homepage deal clearance
    - The original template prose and Process section are preserved (file is appended/edited, not rewritten)
    - This task is sequenced BEFORE Task 3 publishes the deals on the homepage
  </acceptance_criteria>
  <done>The homepage's named deals (Athelas/Commure $6B, Mode Analytics $200M, Illumina/Roche defense, the representative-parties list, and the Daniel Brian testimonial) are each a cleared row attributed to Jon Van Loo / 2026-05-26, and the D-04 clearance is logged in DECISIONS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create the ApproachBand and ChambersStrip section components</name>
  <read_first>
    - src/components/sections/FeeStructureBand.astro (structural analog for ApproachBand: banded surface, accent leading marker idiom `<span class="block h-1 w-10 rounded-full bg-accent" aria-hidden="true">`)
    - src/components/sections/TestimonialQuote.astro (centered-strip analog for ChambersStrip + the `data-component` marker convention)
    - src/components/sections/PracticeAreaCard.astro (3-col grid idiom `grid gap-8 sm:grid-cols-2 lg:grid-cols-3` and accent-icon usage)
    - src/components/ui/Eyebrow.astro (kicker primitive to reuse above the Approach lead line)
    - .planning/phases/03-homepage-static-pages/03-CONTEXT.md (D-02 Approach band intent; Chambers strip = recognition not lead)
    - .planning/phases/03-homepage-static-pages/03-PATTERNS.md (the ApproachBand + ChambersStrip skeletons and the `bg-accent/10` tint idiom)
  </read_first>
  <action>
    Create `src/components/sections/ApproachBand.astro` (D-02). It is the StoryBrand guide-with-a-plan beat directly below the hero: a `<section data-component="ApproachBand" class="bg-bg py-section">` wrapping `mx-auto max-w-6xl px-gutter`. Render an `<Eyebrow>` kicker (e.g. "How we work"), a single situational lead line in `text-h3 font-bold text-text` naming the client's situation ("A complex transaction deserves a team that treats it like their only one." — restrained/premium per D-02), then a 3-up grid (`grid gap-8 sm:grid-cols-1 lg:grid-cols-3`) of three points that MUST map explicitly to the three FIRM_BRIEF client fears: (1) "Partner-led teams" -> competence, (2) "Deep transaction experience" -> experience, (3) "Moves at your deal's pace" -> responsiveness. Each point uses the accent leading marker `<span class="block h-1 w-10 rounded-full bg-accent" aria-hidden="true"></span>` then an `<h3 class="text-h3 font-bold text-text">` and a `text-body text-text-muted` line. Token-only — never `text-[color:var(...)]` or hex. Take no props (content is fixed for the homepage; About reuses it in Plan 02). Create `src/components/sections/ChambersStrip.astro`: a quiet `<section data-component="ChambersStrip" class="bg-bg py-section">` -> `mx-auto max-w-6xl px-gutter` -> an accent-tinted card `rounded-card bg-accent/10 px-6 py-5 text-center text-body text-text` containing the non-linked text "Chambers USA — Spotlight 2026, ranked in Mergers & Acquisitions." (no outbound URL — deferred until known). Keep it visually light: recognition, not the lead.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | Select-String -Pattern "ApproachBand|ChambersStrip|error" | Select-Object -First 5; if (Test-Path src/components/sections/ApproachBand.astro) { if (Test-Path src/components/sections/ChambersStrip.astro) { exit 0 } } ; exit 1</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/ApproachBand.astro` exists with `data-component="ApproachBand"` and three points whose copy names partner-led teams, transaction experience, and deal-pace responsiveness
    - `src/components/sections/ChambersStrip.astro` exists with `data-component="ChambersStrip"`, the string "Chambers USA — Spotlight 2026", and a `bg-accent/10` tint
    - Neither file contains `text-[color:var(` or a hardcoded hex color
    - `astro check` reports no errors introduced by the two new components
  </acceptance_criteria>
  <done>Two new section components exist, token-only, ApproachBand maps three points to competence/experience/responsiveness, ChambersStrip is a quiet accent-tinted recognition strip — both type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: Compose the public homepage in the locked D-01 order</name>
  <read_first>
    - src/pages/index.astro (the file being modified — current Phase 1 placeholder; reuse its title/description props)
    - src/pages/design-system.astro (composition + content source of truth: imports, the `attorneys` and `deals` consts, the 5 headshot imports, Hero copy, the 3 practice teasers, the Daniel Brian testimonial — lines 10-95, 137-202)
    - src/components/sections/ApproachBand.astro and src/components/sections/ChambersStrip.astro (created in Task 2 — the two new sections to slot in)
    - .planning/phases/03-homepage-static-pages/03-PATTERNS.md (the index.astro pattern assignment and the D-01 ordering pitfall: the gallery renders TestimonialQuote BEFORE DealsGrid; D-01 inverts this)
    - .planning/phases/03-homepage-static-pages/03-CONTEXT.md (D-01 final order; D-03 reuse gallery content)
  </read_first>
  <action>
    Rewrite `src/pages/index.astro` to compose the public homepage. Copy from `design-system.astro` the BaseLayout + section imports, the five headshot `import` statements, and the `attorneys` and `deals` consts VERBATIM. Add imports for `ApproachBand` and `ChambersStrip`. DROP the `FeeStructureBand`, `FaqAccordion`, and `faqs` const, the gallery intro `<header>`, the "UI primitives" and "Chrome" demo sections, and the `noindex` prop. Wrap in the public BaseLayout form using the existing placeholder's title="Belcher, Smolen & Van Loo LLP" and the existing description (no noindex). Render sections in EXACTLY the D-01 order: Hero -> ApproachBand -> a "Practice areas" section with the 3 PracticeAreaCard (verbatim hrefs `/practice-areas/mergers-acquisitions`, `/practice-areas/intellectual-property-technology-transactions`, `/practice-areas/tax`) -> DealsGrid heading="Representative work" deals={deals} -> a "The team" section mapping `attorneys` to AttorneyCard with NO href (non-linking in Phase 3) -> TestimonialQuote (Daniel Brian / "GC, Commure, Inc." / the verbatim Athelas-Commure quote) -> ChambersStrip -> CtaBlock heading="Have a transaction in motion?" body and ctaHref="/contact". CRITICAL: DealsGrid comes BEFORE AttorneyCard which comes BEFORE TestimonialQuote — do NOT copy the gallery's TestimonialQuote-then-DealsGrid order. Keep the existing section-wrapper idioms (`bg-bg py-section` + `mx-auto max-w-6xl px-gutter`) for the practice and team grids. Token-only throughout.
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "error" | Select-Object -First 3; node -e "const f=require('fs').readFileSync('src/pages/index.astro','utf8'); const order=['Hero','ApproachBand','PracticeAreaCard','DealsGrid','AttorneyCard','TestimonialQuote','ChambersStrip','CtaBlock']; let i=-1,ok=true; for(const t of order){const j=f.indexOf(t); if(j<=i){ok=false;break} i=j} ok=ok && f.includes('Team work to get good results') && !f.includes('FeeStructureBand') && !f.includes('text-[color:var('); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/index.astro` renders Hero, then ApproachBand, then PracticeAreaCard, then DealsGrid, then AttorneyCard, then TestimonialQuote, then ChambersStrip, then CtaBlock — in that source order (D-01)
    - `src/pages/index.astro` contains the string "Team work to get good results"
    - `src/pages/index.astro` does NOT contain "FeeStructureBand", "FaqAccordion", or `text-[color:var(`
    - The Hero `ctaHref` and the CtaBlock `ctaHref` both point to `/contact`
    - All five attorneys (Belcher, Smolen, Van Loo, Zhang, Jiang) render; Fishbien appears nowhere
    - AttorneyCard instances on the homepage carry no `href` (no "View profile" until Phase 4)
    - `npm run build` succeeds with no errors
  </acceptance_criteria>
  <done>The homepage is the public `/` route, opens with the lead message, presents the full proof set in D-01 order with the two new bands inserted, drives toward `/contact`, and builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time content -> static HTML | All homepage content is authored at build time; there is no user input, no form, no DB on this page. The only "data" is curated consts and JSON-LD injected by BaseLayout. |

## STRIDE Threat Register (ASVS L1, proportionate to read-only static content)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | Homepage named-deal copy (client/counterparty names) | mitigate | D-04 clearance gate (Task 1) — no client/counterparty name ships unless recorded in CLIENT_DISCLOSURE_CLEARANCE.md; this is the Rule 1.6/7.4 control |
| T-03-02 | Tampering | CSP — no inline/third-party script | mitigate | Homepage adds no inline `<script>` and no third-party embed; ChambersStrip is non-linked text (no outbound). Locked `font-src 'self'` and CSP are not loosened |
| T-03-03 | Information disclosure | JSON-LD | accept/N-A | LegalService JSON-LD is injected by BaseLayout as serialized data only (no script execution); no new JSON-LD added here |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs NO packages — all components compose existing Phase 2 sections. No package-legitimacy checkpoint required |
| (auth/session/input L1) | S/R/E | — | N/A | No authentication, sessions, forms, or user input on this page (the contact form is Phase 6) — standard L1 input/auth threats do not apply |
</threat_model>

<verification>
- `npm run build` succeeds and emits `dist/client/index.html`.
- `npm run test:disclaimer` (disclaimer-crawl) still passes — footer disclaimer present on `/`.
- `npm run test:jsonld` still passes — LegalService JSON-LD present on `/` via BaseLayout.
- Clearance register contains all D-04 items before the homepage publishes them.
</verification>

<success_criteria>
- Homepage opens with "Team work to get good results." (PAGES-01)
- Three practice teasers, five attorneys, one marquee testimonial, Chambers Spotlight 2026 strip, primary `/contact` CTA all present (PAGES-02)
- StoryBrand client-as-hero framing with a CTA toward contact (PAGES-08)
- Chambers recognition visible but not the lead (LEGAL-07)
- All named deals cleared in CLIENT_DISCLOSURE_CLEARANCE.md before publish (D-04 gate)
</success_criteria>

<output>
Create `.planning/phases/03-homepage-static-pages/03-01-SUMMARY.md` when done.
</output>
