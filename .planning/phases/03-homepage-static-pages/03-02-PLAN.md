---
phase: 03-homepage-static-pages
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/site.ts
  - src/components/chrome/SiteFooter.astro
  - src/pages/about.astro
autonomous: true
requirements: [PAGES-03, PAGES-08, OPS-06]
must_haves:
  truths:
    - "The About page presents BSV's positioning and substance with the client framed as the hero (StoryBrand), opening with the client's world and the fear of the wrong counsel"
    - "Both office locations render from SITE.offices — 'Silicon Valley, CA' (TBD street omitted) and the full 555 California St., Suite 4925 San Francisco address"
    - "The About page closes with a CTA toward /contact and links onward to /attorneys and /practice-areas"
    - "No invented founding year or origin narrative appears — substance is verifiable facts only (D-06)"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "Client-first StoryBrand About page with two-office block and closing CTA"
      contains: "555 California St"
    - path: "src/lib/site.ts"
      provides: "formatOffice helper DRYing the office-line derivation shared by footer and About"
      contains: "formatOffice"
  key_links:
    - from: "src/pages/about.astro"
      to: "src/lib/site.ts (SITE.offices)"
      via: "formatOffice import + map"
      pattern: "formatOffice|SITE.offices"
    - from: "src/pages/about.astro"
      to: "/contact"
      via: "CtaBlock ctaHref"
      pattern: "ctaHref=\"/contact\""
---

<objective>
Deliver the About page as a complete, shippable vertical slice — a real navigable `/about` page that frames the client as the hero, presents BSV's substance as proof, shows both offices from the single source of truth, and closes the StoryBrand loop toward contact.

Purpose: A prospective client reads BSV's positioning in plain English, sees the firm framed as the trusted guide (team approach, partner access, transaction focus = "the plan"), and is routed onward to contact and the section indexes.
Output: A rewritten `/about` page (token-retrofitted, client-first); a small `formatOffice` helper in `site.ts` that DRYs the office derivation now shared by the footer and About.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-homepage-static-pages/03-CONTEXT.md
@.planning/phases/03-homepage-static-pages/03-PATTERNS.md
@.planning/FIRM_BRIEF.md

<interfaces>
<!-- Source-of-truth shapes for the About page. Extracted from src/lib/site.ts and section components. -->

SITE (src/lib/site.ts): { name, shortName, baseUrl, phone, email, offices: { streetAddress, addressLocality, addressRegion, postalCode, addressCountry }[] }
  — offices[0] = San Francisco (full address, postalCode "94104"); offices[1] = Silicon Valley (streetAddress "TBD", postalCode "TBD")

Office-line derivation (currently duplicated in SiteFooter.astro lines 21-30 — extract to a `formatOffice(o)` helper):
  build segments; push streetAddress only if present and !== "TBD"; push addressLocality; push "region postal" only when non-TBD -> join with ", "
  Produces "Silicon Valley, CA" for the TBD office and "555 California St., Suite 4925, San Francisco, CA 94104" for SF.

CtaBlock: { heading, ctaLabel (req), ctaHref (req), body? }
ApproachBand (from Plan 01): no props — the StoryBrand guide-with-a-plan band; About may reuse it for the "the plan" beat
Eyebrow: slot-only kicker
TextLink: { href } slot-only inline link — use for onward links to /attorneys and /practice-areas
BaseLayout props: { title (req), description (req), canonical?, ogImage?, noindex? } — do NOT pass noindex
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a formatOffice helper into site.ts and refactor SiteFooter to use it</name>
  <read_first>
    - src/lib/site.ts (the file being modified — the SITE const + the existing TBD-handling commentary)
    - src/components/chrome/SiteFooter.astro (lines 21-30 hold the office-line derivation to extract; the render loop at 51-53 must keep producing identical output — do NOT regress the footer disclaimer or office strings)
    - .planning/phases/03-homepage-static-pages/03-PATTERNS.md (the "Planner consideration: a small formatOffice(o) helper would DRY it" note + the exact derivation logic)
  </read_first>
  <action>
    Add an exported `formatOffice(o)` function to `src/lib/site.ts` that takes one office object from `SITE.offices` and returns the formatted single-line string using the existing footer logic: collect segments — push `streetAddress` only when truthy and !== "TBD"; always push `addressLocality`; build a region string from `addressRegion` plus `postalCode` only when postalCode is truthy and !== "TBD", and push it only when non-empty — then `join(', ')`. This must produce "Silicon Valley, CA" for the TBD office and "555 California St., Suite 4925, San Francisco, CA 94104" for SF (behavior identical to the current footer). Then refactor `src/components/chrome/SiteFooter.astro` to import and use `formatOffice` (replace the inline `officeLines` derivation with `const officeLines = SITE.offices.map(formatOffice)`), keeping the `<address>` render loop, the `<Disclaimer id="footer" />` render, the mailto, and the locked container EXACTLY as-is. Do not change any footer markup other than swapping the derivation for the helper call.
  </action>
  <verify>
    <automated>npm run test:disclaimer 2>&1 | Select-String -Pattern "passed|failed|footer disclaimer" | Select-Object -First 3; node -e "const f=require('fs').readFileSync('src/lib/site.ts','utf8'); process.exit(f.includes('formatOffice')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/site.ts` exports a `formatOffice` function
    - `formatOffice(SITE.offices[0])` returns a string containing "555 California St., Suite 4925" and "San Francisco" and "94104"
    - `formatOffice(SITE.offices[1])` returns exactly "Silicon Valley, CA" (no "TBD" substring)
    - `src/components/chrome/SiteFooter.astro` imports `formatOffice` and still renders `<Disclaimer id="footer" />`
    - `npm run test:disclaimer` passes (footer disclaimer not regressed on any route)
  </acceptance_criteria>
  <done>Office-line formatting lives once in `site.ts` as `formatOffice`, the footer consumes it without behavior change, and the disclaimer-crawl test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the About page as a client-first StoryBrand slice</name>
  <read_first>
    - src/pages/about.astro (the file being modified — current placeholder uses the old `text-[color:var(...)]` form to be retrofitted)
    - src/lib/site.ts (the `formatOffice` helper from Task 1 + SITE.offices source of truth)
    - src/pages/design-system.astro (page-shell + heading/body token pattern, lines 103-111: `text-h1`/`text-h2`/`text-h3 font-bold text-text`, body `text-body-lg text-text-muted`)
    - src/components/sections/CtaBlock.astro and src/components/sections/ApproachBand.astro (closing CTA + optional reuse of the Approach band for "the plan" beat)
    - src/components/ui/TextLink.astro and src/components/ui/Eyebrow.astro (onward links + kickers)
    - .planning/phases/03-homepage-static-pages/03-CONTEXT.md (D-05 client-first shape, D-06 verifiable-facts-only, D-07 offices-from-SITE + no map embed + closing loop)
    - .planning/FIRM_BRIEF.md (the ONLY content source for firm substance — practice focus, senior pedigrees, team-approach philosophy, Chambers; do NOT invent a founding year)
  </read_first>
  <action>
    Rewrite `src/pages/about.astro` (PAGES-03/PAGES-08/D-05/D-06/D-07). Keep the BaseLayout wrapper with a real title/description (no noindex). Structure top-to-bottom (StoryBrand spine): (1) an `<Eyebrow>` + an opening narrative in a `max-w-3xl px-gutter` reading column that names the client's world and the fear of the wrong counsel — open with a page-appropriate echo of the homepage framing such as "A complex transaction deserves a team that treats it like their only one." (NOT a verbatim copy of the homepage line); (2) frame BSV as the guide — the team approach, partner access, transaction focus = "the plan" (you MAY reuse the `ApproachBand` component here for this beat, or write prose with the same competence/experience/responsiveness spine); (3) a "why a boutique" proof section drawn STRICTLY from FIRM_BRIEF.md (M&A / IP-Tech / Tax focus, the partners' senior pedigrees, Chambers recognition, senior team) — credentials as proof, never the lead, and NO invented founding year or origin story (D-06); (4) a two-office block in a `max-w-6xl px-gutter` container rendering `SITE.offices.map(formatOffice)` inside an `<address class="not-italic">` (text-only, no third-party map embed per D-07), producing "Silicon Valley, CA" and the full SF address; (5) a closing `CtaBlock` heading toward `/contact` plus inline `<TextLink>` onward links to `/attorneys` and `/practice-areas` (StoryBrand loop close, D-07). Use Phase 2 namespace tokens throughout — headings `text-h1`/`text-h2`/`text-h3 font-bold text-text`, body `text-body`/`text-body-lg text-text-muted`. Remove every `text-[color:var(...)]` occurrence. Because content lives in this `.astro` file, keep the prose blocks plainly editable (clear paragraphs) so a non-coding author can edit them via the GitHub web editor (OPS-06).
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "error" | Select-Object -First 3; node -e "const f=require('fs').readFileSync('src/pages/about.astro','utf8'); const ok=(f.includes('formatOffice')||f.includes('SITE.offices')) && !f.includes('text-[color:var(') && f.includes('/contact'); process.exit(ok?0:1)"; node -e "const h=require('fs').readFileSync('dist/client/about/index.html','utf8'); const ok=['555 California St','Silicon Valley','/contact','/attorneys','/practice-areas'].every(s=>h.includes(s)) && !h.includes('Suite 4925, San Francisco, CA 94104, TBD'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/about.astro` renders both office lines via `formatOffice` / `SITE.offices` — rendered `/about` HTML contains "555 California St" and "Silicon Valley" with no stray "TBD" office string
    - The page opens with client-world framing (the fear of the wrong counsel), NOT with credentials or a founding year
    - `src/pages/about.astro` contains NO invented founding year / origin narrative (substance only from FIRM_BRIEF.md)
    - The page closes with a CtaBlock to `/contact` and inline TextLinks to `/attorneys` and `/practice-areas`
    - `src/pages/about.astro` contains no `text-[color:var(` occurrence (token retrofit complete)
    - `npm run build` succeeds with no errors
  </acceptance_criteria>
  <done>`/about` is a real, client-first StoryBrand page rendering both offices from the source of truth, free of invented history, token-retrofitted, and closing the loop toward contact and the indexes — and builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time content -> static HTML | All About content is authored at build time from SITE constants and FIRM_BRIEF prose; no user input, no form, no DB. |

## STRIDE Threat Register (ASVS L1, proportionate to read-only static content)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information disclosure | Office address rendering | mitigate | Offices render only from `SITE.offices`; the TBD Silicon Valley street is suppressed by `formatOffice` so no placeholder/partial address leaks (Task 1 + render assertion) |
| T-03-05 | Tampering | CSP — no map embed, no inline/third-party script | mitigate | D-07 forbids a third-party map embed; About adds no inline `<script>` and no external embed, so the locked CSP (`font-src 'self'`) is not loosened |
| T-03-06 | Information disclosure | Firm-substance copy | mitigate | D-06 verifiable-facts-only constraint — no fabricated history that could later read as a misleading professional claim |
| T-03-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs NO packages — composes existing components + adds one local helper. No package-legitimacy checkpoint required |
| (auth/session/input L1) | S/R/E | — | N/A | No authentication, sessions, forms, or user input on this page |
</threat_model>

<verification>
- `npm run build` succeeds and emits `dist/client/about/index.html`.
- `npm run test:disclaimer` still passes — footer disclaimer present on `/about` (and the footer refactor did not regress it).
- Rendered `/about` shows both offices correctly (full SF address; "Silicon Valley, CA" with no TBD).
</verification>

<success_criteria>
- About covers positioning, substance, and both offices in plain English with the client as hero (PAGES-03)
- StoryBrand framing with a CTA toward contact and onward links (PAGES-08)
- Editable prose in a file Jon can edit via the GitHub web editor (OPS-06)
- No invented founding year (D-06); offices from SITE source of truth, no map embed (D-07)
</success_criteria>

<output>
Create `.planning/phases/03-homepage-static-pages/03-02-SUMMARY.md` when done.
</output>
