<svg viewBox="0 0 48 48" fill="none" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round" xmlns="http://www.w3.org/2000/svg">
  <!-- Tax: a structured / ledger mark -- converging lines forming an ordered
       structure (the "structuring that makes the deal work"). Thin-line,
       currentColor only; same line system as the hero + other practice icons.
       Decorative (aria-hidden at call site). -->
  <!-- Ledger frame -->
  <path d="M12 9 H36 V39 H12 Z" />
  <!-- Structured ruled rows (the ordered structuring) -->
  <path d="M17 17 H31" />
  <path d="M17 23 H31" />
  <path d="M17 29 H26" />
  <!-- Converging diagonals resolving to a single structured outcome -->
  <path d="M12 9 L24 19 L36 9" />
</svg>
