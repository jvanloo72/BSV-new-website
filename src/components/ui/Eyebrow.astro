---
/* Eyebrow primitive — small uppercase kicker label placed above section
   headings (UI-SPEC §"UI primitives"). Token-only — text-small + muted color. */
---

<p class="text-small uppercase tracking-wide text-text-muted">
  <slot />
</p>
